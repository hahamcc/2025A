# 问题 1：烟幕干扰弹有效遮蔽时长的建模思路

> **实施更新（以 `Q1/q1_main.py` 的实际计算为准）**：正式程序使用“完整圆柱外表面采样”的有限视线段—球体相交判据。同时加入了上、下底面双圆周快速评价器。两者在每圈 360、720、1440 点的对比中都得到相同结果：有效区间为 \([8.0564454894,\,9.4480881587]\) s，总时长为 \(1.3916426693\) s。后续优化可以用双圆周加速，但最终候选必须回到完整外表面版本复核。

> **判定补充**：是否遮挡只由“导弹到目标点的有限线段是否与烟幕球相交”决定。导弹若在烟幕球内，视线从烟幕内部出发，仍应计入遮挡；不能只看未截断投影参数的正负来排除该情况。

## 一、简单路径总结

本问不是优化题，而是**按题目给定策略进行几何仿真**：

1. 以接令时刻为 `t=0`，写出 FY1、干扰弹、烟幕云团和 M1 导弹的运动轨迹；
2. 算出投放点、起爆点，以及烟幕有效的时间窗；
3. 在每个时刻判断：烟幕球是否挡住导弹看向真实圆柱目标的全部视线；
4. 先用圆柱上下边缘圆周的采样点计算遮蔽状态，粗扫找到遮蔽区间；
5. 对遮蔽开始、结束的临界时刻做二分精修；
6. 再用遮挡锥的一维半解析判据独立复核；
7. 比较不同采样密度下的结果，确认时长已收敛。

最终输出：投放点、起爆点、有效遮蔽的起止时刻、总有效遮蔽时长，以及两种计算方式的对照结果。

---

## 二、建模口径与基本假设

### 2.1 题面直接给定的规则

- 导弹 M1 以 `300 m/s` 匀速直线飞向假目标原点，不考虑重力和机动；
- FY1 接令后以 `120 m/s` 朝假目标飞行；
- 干扰弹脱离 FY1 后继承其水平速度，并受重力作用；
- 干扰弹爆炸后，云团瞬时形成半径为 `10 m` 的球；
- 云团只以 `3 m/s` 匀速下沉，起爆后 `20 s` 内有效；
- 取重力加速度 `g=9.8 m/s^2`。

### 2.2 本文采用的“完全遮蔽”定义

在某时刻，若从导弹位置看向真实圆柱目标的任一视线都先经过烟幕球，则称该时刻实现了完全遮蔽。

这是一个偏严格的口径：它要求烟幕形成的遮挡区域完整包住真实圆柱，而不只是挡住圆柱中心点。其优点是定义清楚、可复核；其代价是所得有效时长可能比“仅遮蔽中心点”的口径更短。

---

## 三、坐标、时间与运动轨迹

以雷达发现导弹、无人机接令的时刻为 `t=0`。

### 3.1 真实目标

真实目标为半径 `7 m`、高度 `10 m` 的竖直圆柱体，下底面圆心为 `(0, 200, 0)`：

\[
\mathcal T=\{(x,y,z):x^2+(y-200)^2\le 7^2,\ 0\le z\le10\}.
\]

### 3.2 FY1 与干扰弹

FY1 初始位置为：

\[
U_0=(17800,0,1800).
\]

它以 `120 m/s` 沿 x 轴负方向飞向假目标，因此：

\[
v_U=(-120,0,0).
\]

投放时刻为：

\[
t_d=1.5\ \mathrm{s}.
\]

故投放点为：

\[
P_d=U_0+v_Ut_d=(17620,0,1800).
\]

起爆延时为 `3.6 s`，故起爆时刻：

\[
t_b=t_d+3.6=5.1\ \mathrm{s}.
\]

在投放至起爆之间，干扰弹运动为：

\[
B(t)=P_d+v_U(t-t_d)+(0,0,-\tfrac12g(t-t_d)^2),
\quad t_d\le t\le t_b.
\]

因此起爆点为：

\[
P_b=B(t_b)
=\left(17188,0,1800-\tfrac12\times9.8\times3.6^2\right)
\approx(17188,0,1736.496).
\]

### 3.3 烟幕云团

起爆后，云团中心从起爆点开始以 `3 m/s` 向下沉降：

\[
S(t)=(17188,0,1736.496-3(t-5.1)),
\quad 5.1\le t\le25.1.
\]

因此，只有时间窗

\[
[5.1,25.1]\ \mathrm{s}
\]

内可能出现有效遮蔽。

### 3.4 导弹 M1

导弹初始位置：

\[
M_0=(20000,0,2000).
\]

导弹始终指向假目标原点，故其位置为：

\[
M(t)=M_0+300t\frac{-M_0}{\|M_0\|}.
\]

---

## 四、遮蔽判据：视线线段与烟幕球相交

对真实圆柱上的任一点 `Q`，导弹到该点的视线为线段 `M(t)Q`。

先计算烟幕中心在这条线段上的投影参数：

\[
\lambda=
\frac{(S(t)-M(t))\cdot(Q-M(t))}
{\|Q-M(t)\|^2}.
\]

再将其截断到 `[0,1]`：

\[
\lambda_c=\operatorname{clip}(\lambda,0,1).
\]

视线线段上离烟幕中心最近的点为：

\[
P=M(t)+\lambda_c(Q-M(t)).
\]

若

\[
\|S(t)-P\|\le10,
\]

说明导弹看向点 `Q` 的视线与烟幕球相交，点 `Q` 被遮挡。

若所有待检目标点均被遮挡，则该时刻为有效遮蔽时刻。有限线段判据会自动排除“烟幕只落在视线延长线而不在线段上”的误判；导弹若位于烟幕球内，也会正确判为相交。

---

## 五、为何检查上下两条圆周

真实圆柱的上、下边缘圆周写为：

\[
Q_z(\theta)=(7\cos\theta,\ 200+7\sin\theta,\ z),
\]

其中：

\[
z\in\{0,10\},\qquad 0\le\theta<2\pi.
\]

当烟幕位于导弹和目标之间时，导弹对烟幕球作出的切线构成一个实心遮挡锥。该实心锥是凸集；而圆柱可由其上下边缘圆周生成。若两条边缘圆周均落在遮挡锥内，则整个圆柱也落在遮挡锥内。

因此，本问可以把原本三维的“检查整个圆柱”降为两条一维圆周上的检查。

> 注意：遮挡锥表述只适合导弹在烟幕球外、用切线说明临界状态的情形。正式程序始终使用有限线段—球体相交判据，因此也覆盖导弹进入烟幕球的特殊时刻，不能只检查无限延长的锥体。

本段基础概念可由凸分析教材支撑；针对本题圆柱与遮挡锥的结论，是本文在题面条件下给出的几何推论。

---

## 六、三种候选计算方案

| 方案 | 做法 | 优点 | 局限 |
|---|---|---|---|
| 全时空密集网格 | 圆柱内部大量取点，时间也取很小步长 | 最直观 | 计算冗余，精度依赖网格和时间步 |
| 完整圆柱外表面采样 + 临界时刻精修 | 采样完整外表面；粗扫后对边界二分 | 与“完整目标”口径直接一致 | 点数多，优化阶段较慢 |
| 双圆周采样 + 临界时刻精修 | 上下圆周取点；粗扫时间后对边界二分 | 已与完整外表面核验一致，速度快 | 仍须将最终候选回到完整外表面复核 |
| 遮挡锥临界几何说明 | 用视线相切解释遮挡状态的边界 | 图形直观，帮助论文说明 | 不是独立替代正式线段判据的物理模型 |

### 本问的最终分工

- **正式计算**：完整圆柱外表面采样 + 临界时刻二分；
- **快速评价**：双圆周采样 + 临界时刻二分；
- **几何说明**：遮挡锥用于解释相切边界，不和采样法并列为两套独立物理模型。

---

## 七、主计算流程：圆周采样与时间精修

### 7.1 圆周采样

在每条圆周上取 `N` 个等间隔点：

\[
\theta_j=\frac{2\pi j}{N},\quad j=0,1,\ldots,N-1.
\]

建议至少使用三组采样密度：

| 采样数 `N` | 圆周相邻点间距约为 |
|---:|---:|
| 360 | 0.122 m |
| 720 | 0.061 m |
| 1440 | 0.031 m |

每个时刻共检查 `2N` 个圆周点。

### 7.2 时间扫描与边界精修

1. 在 `[5.1,25.1]` s 内以较粗步长（如 `0.05 s`）逐时刻检查是否完全遮蔽；
2. 找到“未遮蔽到遮蔽”以及“遮蔽到未遮蔽”的相邻时间段；
3. 对每个变化段使用二分法，将临界时刻精确到 `10^-4 s`；
4. 允许出现多个有效区间，最后求全部区间的并集长度。

二分法只用于精确定位边界；粗时间步只负责找到边界所在的小区间，因此最终精度不由 `0.05 s` 决定。

---

## 八、遮挡锥的临界几何说明

在任意时刻 `t`，令：

\[
d(t)=\|S(t)-M(t)\|,\qquad
e(t)=\frac{S(t)-M(t)}{d(t)}.
\]

其中 `e(t)` 为遮挡锥的轴向单位向量。以下切锥公式只在 \(d(t)>10\) m、即导弹位于烟幕球外时用于说明相切边界；导弹进入球内时不使用它作判断，仍回到正式的有限线段判据。烟幕球半径为 `10 m`，故遮挡锥半顶角满足：

\[
\cos\alpha(t)=\sqrt{1-\left(\frac{10}{d(t)}\right)^2}.
\]

对圆周点定义遮挡裕量：

\[
G_z(\theta,t)=
\frac{e(t)\cdot(Q_z(\theta)-M(t))}
{\|Q_z(\theta)-M(t)\|}
-\cos\alpha(t).
\]

- 若 `G_z(θ,t) ≥ 0`，该点在遮挡锥内；
- 若 `G_z(θ,t) < 0`，该点露在遮挡锥外。

定义：

\[
G(t)=\min_{z\in\{0,10\},\theta\in[0,2\pi)}G_z(\theta,t).
\]

当 `G(t) ≥ 0`，它说明圆柱视线位于切锥所描述的遮挡侧，是“状态将要改变时发生相切”的几何解释。

该量不取代主方法中的“线段—球相交”判据，也不作为独立数值结果。它用于绘制示意图、解释临界点；正式的起止时刻与总时长均由有限线段—球体相交评价器输出。

---

## 九、验证与结果表达

第一问至少完成以下检查：

1. **时间窗检查**：`t<5.1 s` 或 `t>25.1 s` 时，遮蔽必为无效；
2. **有限线段检查**：对每一个判为遮蔽的目标点，最近点必须落在导弹—目标的有限线段上；导弹位于烟幕球内的情形由该判据自然覆盖；
3. **采样收敛检查**：比较 `N=360,720,1440` 时的开始时刻、结束时刻和时长；
4. **评价器对照**：完整圆柱外表面与双圆周在多个采样密度下的结果应一致；
5. **边界检查**：遮蔽开始与结束时刻，应对应至少一个目标边缘点恰好与烟幕球相切或恰位于遮挡锥边界。

建议用下表汇报：

| 采样密度/方法 | 遮蔽开始时刻 | 遮蔽结束时刻 | 有效遮蔽时长 | 备注 |
|---|---:|---:|---:|---|
| 双圆周采样 `N=360` |  |  |  | 快速评价 |
| 双圆周采样 `N=720` |  |  |  | 快速评价 |
| 双圆周采样 `N=1440` |  |  |  | 高密度快速评价 |
| 完整圆柱外表面 |  |  |  | 正式结果 |

## 十、实施结果、评价器分工与后续复用

### 10.1 已核验的正式结果

在题设给定 FY1 策略下，完整圆柱外表面和双圆周均得到

\[
[t_{\rm start},t_{\rm end}]=[8.0564454894,\,9.4480881587]\ \text{s},
\]

\[
T_{\rm eff}=1.3916426693\ \text{s}.
\]

点目标简化结果为 \(1.4101973994\) s，只能作为简化基线，不能代替完整圆柱的正式结果。

### 10.2 两种评价器的分工

当前完整圆柱外表面有 148320 个采样点；双圆周每圈 1440 点，共 2880 个点，点数约减少 98.06%。因此后续问题建议按下表使用。

| 场景 | 建议评价器 |
|---|---|
| 问题一正式结果 | 完整圆柱外表面 |
| 问题二至四的粗搜索 | 低/中密度双圆周 |
| 候选方案重排序 | 高密度双圆周 |
| 最终报告的最优候选 | 完整圆柱外表面 |

### 10.3 遮挡锥的准确定位

从导弹位置向烟幕球作切线，可得到遮挡锥边界；目标点的视线恰为切线时，正是遮挡状态将改变的临界情形。它解释了为什么正式判据要检查“相交或相切”。数值计算仍以有限线段—球体相交为准，不把遮挡锥和表面采样写成两套互不相关的物理模型。

## 十一、可写入论文的引用

凸集、凸包和凸锥的基础定义，可引用：

> Boyd, S., & Vandenberghe, L. *Convex Optimization*. Cambridge University Press, 2004, §2.1.4--§2.1.5. https://www.stanford.edu/~boyd/cvxbook/bv_cvxbook.pdf

正文中应如实表述为“借鉴凸分析的基础结论，本文在题设条件下推导出圆柱边缘检查规则”，而不是将本题的几何推论写成文献中的现成结论。
