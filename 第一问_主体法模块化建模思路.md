# 2025A 第一问：基于主体法的模块化建模思路

## 1. 问题定位

第一问给定了无人机、导弹和烟幕干扰弹的全部运动参数，要求计算一枚烟幕干扰弹对 M1 的有效遮蔽时长。因此，本问不是优化问题，而是一个**确定参数下的运动学—动态几何遮蔽计算问题**。

其基本计算链条为：

\[
\boxed{
\text{导弹轨迹}
\longrightarrow
\text{无人机轨迹}
\longrightarrow
\text{投放点与起爆点}
\longrightarrow
\text{烟幕云团轨迹}
\longrightarrow
\text{动态视线遮蔽判定}
\longrightarrow
\text{有效时间区间}
}
\]

本问最合适的建模原则是：

1. 先将真实圆柱目标简化为一个目标特征点，建立容易理解和验证的基础模型；
2. 再恢复圆柱体的半径与高度，对完整圆柱表面进行采样，并要求整个目标均被遮蔽；
3. 比较两个层次的计算结果，检验简化模型的合理性；
4. 最终给出一个明确的主结果，而不是把不同判据得到的数值含糊地当成普通误差区间。

已有研究通常将烟幕投放问题拆分为烟幕弹外弹道、三维烟幕空间位置、被保护目标和遮蔽效能判定等部分，这与本文采用的主体法模块化结构一致[[1]](#参考文献)。将烟幕遮蔽效应嵌入来袭弹药的运动仿真，也是相关研究常用的建模方式[[2]](#参考文献)。

---

## 2. 建模假设

为避免第一问被不必要地复杂化，作如下简化假设：

1. 以警戒雷达发现来袭导弹的时刻为 \(t=0\)，所有主体使用同一时间轴。
2. 导弹和无人机均视为质点。
3. M1 以 \(300\,\mathrm{m/s}\) 的速度沿初始位置到假目标原点的直线匀速飞行。
4. FY1 在受领任务后立即将航向调整为朝向假目标的水平投影方向，随后以 \(120\,\mathrm{m/s}\) 等高度匀速直线飞行。
5. 烟幕干扰弹脱离无人机时继承无人机的水平速度；忽略空气阻力和风场，只受重力作用，取 \(g=9.8\,\mathrm{m/s^2}\)。
6. 烟幕弹起爆后瞬时形成半径为 \(10\,\mathrm{m}\) 的球状有效烟幕区域。
7. 按题意，起爆后的烟幕云团不再保持原水平速度，其球心水平坐标不变，只以 \(3\,\mathrm{m/s}\) 竖直向下匀速运动。
8. 烟幕云团只在起爆后的 \(20\,\mathrm{s}\) 内有效。
9. 只要导弹到目标的视线段与有效烟幕球相交，就认为对应目标点被遮蔽；不额外引入透过率、浓度衰减、风场扩散和导引头识别概率模型。

第 9 条是本题最重要的适当简化。实际烟幕效能可以采用透过率、图像质量、导引头工作性能等多种指标进行评价[[3]](#参考文献)，但题目已经直接给出“云团中心 \(10\,\mathrm{m}\) 范围内、起爆后 \(20\,\mathrm{s}\) 有效”，因此第一问没有必要再建立复杂的烟幕扩散模型。

---

## 3. 基于主体法的模块化建模

本文强调采用**主体法**：先分别描述五个主体，再建立主体之间唯一需要重点分析的“遮蔽关系”。

五个主体分别是：

- 主体一：来袭导弹 M1；
- 主体二：无人机 FY1；
- 主体三：烟幕干扰弹；
- 主体四：起爆后的烟幕云团；
- 主体五：真实圆柱目标。

### 3.1 主体一：来袭导弹 M1

M1 的初始位置为

\[
\boldsymbol M_0=(20000,0,2000).
\]

其飞行方向直指假目标原点 \(O=(0,0,0)\)，因此单位方向向量为

\[
\boldsymbol e_M
=-
\frac{(20000,0,2000)}
{\sqrt{20000^2+2000^2}}.
\]

M1 的位置为

\[
\boldsymbol M(t)
=\boldsymbol M_0+300t\boldsymbol e_M,
\]

即

\[
\boldsymbol M(t)
=
\left(
1-\frac{300t}{\sqrt{20000^2+2000^2}}
\right)(20000,0,2000).
\]

M1 到达假目标的时刻为

\[
T_{M1}
=\frac{\sqrt{20000^2+2000^2}}{300}
\approx66.9992\,\mathrm{s}.
\]

### 3.2 主体二：无人机 FY1

FY1 的初始位置为

\[
\boldsymbol F_0=(17800,0,1800).
\]

由于无人机必须等高度飞行，“朝向假目标方向”应理解为沿水平面朝向原点的投影方向，即沿 \(x\) 轴负方向飞行。其位置为

\[
\boldsymbol F(t)=(17800-120t,0,1800).
\]

FY1 在受领任务 \(1.5\,\mathrm{s}\) 后投放烟幕干扰弹，因此投放时刻和投放点分别为

\[
t_r=1.5\,\mathrm{s},
\]

\[
\boldsymbol P_r
=\boldsymbol F(1.5)
=(17620,0,1800).
\]

### 3.3 主体三：烟幕干扰弹

设烟幕弹投放后经过的时间为

\[
\tau=t-t_r.
\]

烟幕弹继承 FY1 的水平速度 \((-120,0,0)\)，竖直初速度为零。投放后的运动轨迹为

\[
\boldsymbol B(t)
=
\left(
17620-120\tau,
0,
1800-\frac12g\tau^2
\right).
\]

题目规定投放后间隔 \(3.6\,\mathrm{s}\) 起爆，因此起爆时刻为

\[
t_e=t_r+3.6=5.1\,\mathrm{s}.
\]

将 \(\tau=3.6\) 和 \(g=9.8\) 代入，得到起爆点

\[
\begin{aligned}
x_e&=17620-120\times3.6=17188,\\
y_e&=0,\\
z_e&=1800-\frac12\times9.8\times3.6^2=1736.496.
\end{aligned}
\]

因此

\[
\boxed{
\boldsymbol P_e=(17188,0,1736.496)
}.
\]

### 3.4 主体四：烟幕云团

烟幕弹起爆后瞬时形成球状烟幕云团。有效烟幕球的半径为

\[
R_s=10\,\mathrm{m}.
\]

云团球心以 \(3\,\mathrm{m/s}\) 的速度竖直下沉，所以

\[
\boldsymbol C(t)
=
\left(
17188,
0,
1736.496-3(t-5.1)
\right).
\]

云团仅在起爆后 \(20\,\mathrm{s}\) 内有效，即

\[
5.1\le t\le25.1.
\]

虽然 M1 直到约 \(66.9992\,\mathrm{s}\) 才到达原点，但烟幕在 \(25.1\,\mathrm{s}\) 后失效，因此第一问只需在区间 \([5.1,25.1]\) 内进行遮蔽判断。

### 3.5 主体五：真实圆柱目标

真目标下底面圆心为 \((0,200,0)\)，半径为 \(7\,\mathrm{m}\)，高度为 \(10\,\mathrm{m}\)，因此目标集合为

\[
\mathcal T
=
\left\{
(x,y,z):
x^2+(y-200)^2\le49,
\ 0\le z\le10
\right\}.
\]

针对这个主体，采用两个层次进行处理：

- 第一层：将目标简化为题目给出的下底面圆心 \(\boldsymbol P=(0,200,0)\)；
- 第二层：恢复完整圆柱体，对其表面采样，并要求整个目标均被遮蔽。

这种“先简化、后校核”的处理既能保持第一问的直观性，又不会完全忽略题目给出的半径和高度。

---

## 4. 主体之间的核心关系：动态视线遮蔽

第一问真正需要建立的主体间关系只有一个：

> 烟幕球是否位于 M1 与真实目标之间，并与 M1 指向目标的视线相交。

### 4.1 点目标简化模型

先将真目标简化为特征点

\[
\boldsymbol P=(0,200,0).
\]

在时刻 \(t\)，M1 到目标特征点的视线段为

\[
\boldsymbol L(s,t)
=
\boldsymbol M(t)
+s[\boldsymbol P-\boldsymbol M(t)],
\qquad 0\le s\le1.
\]

烟幕球心在视线段上的投影参数为

\[
\lambda(t)
=
\frac{
[\boldsymbol C(t)-\boldsymbol M(t)]
\cdot
[\boldsymbol P-\boldsymbol M(t)]
}
{\|\boldsymbol P-\boldsymbol M(t)\|^2}.
\]

为了防止将位于导弹后方或目标后方的烟幕误判为有效，应使用“点到线段距离”，而不是无限直线距离。令

\[
\widehat\lambda(t)
=\min\{1,\max\{0,\lambda(t)\}\},
\]

则烟幕球心到视线段的最短距离为

\[
d_P(t)
=
\left\|
\boldsymbol C(t)
-
\left(
\boldsymbol M(t)
+\widehat\lambda(t)[\boldsymbol P-\boldsymbol M(t)]
\right)
\right\|.
\]

点目标模型下的有效遮蔽条件为

\[
\boxed{
d_P(t)\le10,
\qquad 5.1\le t\le25.1
}.
\]

定义示性函数

\[
I_P(t)
=
\begin{cases}
1,&d_P(t)\le10,\\
0,&d_P(t)>10.
\end{cases}
\]

则点目标模型下的有效遮蔽时长为

\[
T_P
=
\int_{5.1}^{25.1}I_P(t)\,\mathrm dt.
\]

### 4.2 完整圆柱表面采样模型

点目标模型没有直接使用圆柱半径和高度，因此在完成基础计算后，需要恢复完整目标进行严格校核。

圆柱侧面可以参数化为

\[
\boldsymbol Q(\varphi,z)
=
(7\cos\varphi,\ 200+7\sin\varphi,\ z),
\]

其中

\[
0\le\varphi<2\pi,
\qquad
0\le z\le10.
\]

上下端面可以参数化为

\[
\boldsymbol Q(r,\varphi,z_c)
=
(r\cos\varphi,\ 200+r\sin\varphi,\ z_c),
\]

其中

\[
0\le r\le7,
\qquad
z_c\in\{0,10\}.
\]

将圆柱侧面和上下端面离散为采样点

\[
\boldsymbol Q_1,\boldsymbol Q_2,\ldots,\boldsymbol Q_N.
\]

对每一个采样点 \(\boldsymbol Q_n\)，构造 M1 到该点的视线段，并计算烟幕球心到该线段的最短距离

\[
d_n(t)
=
\operatorname{dist}
\left(
\boldsymbol C(t),
[\boldsymbol M(t),\boldsymbol Q_n]
\right).
\]

题目要求“整个目标均被遮蔽”时，必须保证所有目标采样点的视线都与烟幕球相交，即

\[
d_n(t)\le10,
\qquad n=1,2,\ldots,N.
\]

将其合并为一个判定函数

\[
D_{\mathrm{cyl}}(t)
=
\max_{1\le n\le N}d_n(t).
\]

于是完整圆柱的严格遮蔽条件为

\[
\boxed{
D_{\mathrm{cyl}}(t)\le10,
\qquad5.1\le t\le25.1
}.
\]

这里使用最大值的含义非常明确：只要存在一个目标采样点的视线没有穿过烟幕球，就不能认为整个圆柱目标已经被完全遮蔽。

相关烟幕部署研究在三维空间中以完整被保护目标和有效遮蔽区域作为约束[[1]](#参考文献)，也有研究利用解析几何和多点施放模型处理完整目标的烟幕遮蔽[[4]](#参考文献)。因此，完整圆柱表面采样是对点目标简化的一种直观、稳妥且不复杂的修正。

---

## 5. 所用算法

第一问不需要任何智能优化算法。最适合的算法组合为：

### 5.1 解析运动学计算

直接使用匀速直线运动和平抛运动公式计算：

- M1 的实时位置；
- FY1 的实时位置；
- 烟幕弹投放点；
- 烟幕弹起爆点；
- 烟幕球心的实时位置。

### 5.2 点到线段距离算法

对任一目标点，利用向量投影计算烟幕球心到“导弹—目标点”视线段的最短距离，并通过截断投影参数到 \([0,1]\) 保证判断对象是线段而不是无限直线。

### 5.3 粗时间扫描

在

\[
[5.1,25.1]
\]

内先取较小时间步长，如

\[
\Delta t=0.01\,\mathrm{s},
\]

逐时刻计算

\[
f(t)=d_P(t)-10
\]

或

\[
f_{\mathrm{cyl}}(t)
=D_{\mathrm{cyl}}(t)-10.
\]

函数由正变负表示进入有效遮蔽状态，由负变正表示离开有效遮蔽状态。粗扫描只用于定位两个边界所在的小区间。

### 5.4 二分法精确求边界

在粗扫描确定的两个符号变化区间内，分别用二分法求解

\[
f(t)=0.
\]

得到进入时刻 \(t_{\mathrm{in}}\) 和离开时刻 \(t_{\mathrm{out}}\)，最终计算

\[
T_{\mathrm{eff}}
=t_{\mathrm{out}}-t_{\mathrm{in}}.
\]

二分法无需计算导数，算法稳定、实现简单、精度容易控制，是第一问最合适的边界求解方法。

### 5.5 圆柱表面规则采样与收敛校验

圆柱表面采用规则网格采样。可以先使用较疏网格完成程序调试，再逐渐增加角度、高度和半径方向的采样数，观察有效遮蔽时长是否稳定。

本次高密度数值复核采用：

- 圆周方向 1441 个等距角度点；
- 圆柱侧面高度方向 101 个等距点；
- 上下端面半径方向各 51 个等距点；
- 时间边界由 60 次二分迭代求得。

继续增加采样密度后，结果在给出的四位小数范围内稳定。因此，圆柱表面离散误差对本问结论影响很小。

### 5.6 算法流程

```text
输入题目参数
    │
    ├─ 建立 M1 运动方程
    ├─ 建立 FY1 运动方程
    ├─ 计算投放点
    ├─ 计算起爆时刻与起爆点
    └─ 建立烟幕球心下沉方程
             │
             ▼
      第一层：点目标简化
             │
      计算球心到视线段距离
             │
      粗扫描 + 二分法
             │
      得到 T_point
             │
             ▼
      第二层：完整圆柱校核
             │
      生成圆柱表面采样点
             │
      计算所有视线段距离的最大值
             │
      粗扫描 + 二分法
             │
      得到 T_cylinder
             │
             ▼
      比较两种结果并确定最终口径
```

---

## 6. 两个层次的计算结果

### 6.1 情形一：目标特征点简化

将真目标简化为下底面圆心

\[
\boldsymbol P=(0,200,0)
\]

后，求解

\[
d_P(t)=10
\]

得到有效遮蔽区间约为

\[
\boxed{
[8.0378907593,\ 9.4480881587]\,\mathrm{s}
}.
\]

因此点目标简化模型得到

\[
\boxed{
T_P
=9.4480881587-8.0378907593
=1.4101973994\,\mathrm{s}
\approx1.4102\,\mathrm{s}
}.
\]

### 6.2 情形二：完整圆柱严格遮蔽

对完整圆柱侧面及上下端面进行高密度规则采样，并要求所有采样点均被烟幕球遮蔽，得到有效遮蔽区间约为

\[
\boxed{
[8.0564454869,\ 9.4480881587]\,\mathrm{s}
}.
\]

因此完整圆柱模型得到

\[
\boxed{
T_{\mathrm{cyl}}
=9.4480881587-8.0564454869
=1.3916426719\,\mathrm{s}
\approx1.3916\,\mathrm{s}
}.
\]

### 6.3 两个结果的比较

两种模型的结果差为

\[
\Delta T
=1.4101973994-1.3916426719
\approx0.01855\,\mathrm{s}.
\]

相对差异约为 \(1.3\%\)。这表明：

1. 点目标模型能够正确反映第一问的主要几何关系；
2. 将圆柱简化为特征点造成的误差较小；
3. 但完整圆柱模型使用了题目给出的半径和高度，且符合“整个目标均被遮蔽”的严格含义，因此物理解释更加完整。

---

## 7. 最终结果的处理建议

两种结果不能直接混合为一个普通数值误差区间。即不建议只写

\[
T_{\mathrm{eff}}\in[1.39,1.4102]\,\mathrm{s},
\]

因为两个端点不是由同一个模型的计算误差产生，而是由两种不同遮蔽口径产生：

- \(1.4102\,\mathrm{s}\)：目标特征点被遮蔽；
- \(1.3916\,\mathrm{s}\)：完整圆柱目标全部被遮蔽。

建议在论文中按照以下方式处理。

### 7.1 基础模型结果

先报告：

> 将真目标简化为其下底面圆心，利用烟幕球心到 M1—目标视线段的距离判定遮蔽状态，得到有效遮蔽时长为 \(1.4102\,\mathrm{s}\)。

该结果用于说明模型、公式和程序的基本正确性。

### 7.2 严格模型结果

随后报告：

> 恢复真目标的圆柱几何形状，对圆柱侧面和上下端面进行规则采样，并要求所有目标采样点均被烟幕球遮蔽，得到有效遮蔽时长为 \(1.3916\,\mathrm{s}\)。

### 7.3 最终主结果

由于题目明确给出了真目标的半径和高度，且本文最终采用“整个圆柱目标均被遮蔽”的严格定义，因此建议把

\[
\boxed{
T_{\mathrm{eff}}\approx1.39\,\mathrm{s}
}
\]

作为第一问的最终主结果。

同时将 \(1.4102\,\mathrm{s}\) 保留为点目标简化结果和模型合理性校核。可以在结果分析中补充：

> 两种合理建模口径给出的结果均稳定在 \(1.39\sim1.41\,\mathrm{s}\)，相对差异约为 \(1.3\%\)，说明点目标简化能够较好地近似完整圆柱遮蔽过程；考虑题目给出的目标几何尺寸后，最终取完整圆柱严格遮蔽时长 \(1.39\,\mathrm{s}\)。

这种写法同时保留了简化模型的直观性和完整圆柱模型的严谨性，也避免将“模型口径差异”误写成“数值误差区间”。

---

## 8. 本问不建议采用的方法

第一问参数全部给定，因此不建议使用：

- 粒子群算法；
- 遗传算法；
- 差分进化算法；
- 模拟退火算法；
- 混合整数规划；
- 复杂流体力学烟幕扩散模型；
- 导引头识别概率或毁伤概率模型。

这些方法要么用于参数寻优，要么用于更复杂的物理效能评价。第一问使用它们不仅不会改善核心结论，反而会削弱模型的可解释性。

第一问真正需要的算法只有：

\[
\boxed{
\text{解析运动学}
+\text{点到线段距离}
+\text{时间扫描}
+\text{二分法}
+\text{圆柱表面规则采样}
}.
\]

---

## 9. 第一问建模结论

基于主体法，可以将第一问清晰地拆分为 M1、FY1、烟幕干扰弹、烟幕云团和真实圆柱目标五个主体。各主体分别建立运动或几何模型，最终通过“烟幕球是否切断导弹到目标的视线”建立联系。

整个建模过程遵循：

\[
\boxed{
\text{先点目标简化}
\longrightarrow
\text{建立并验证基础模型}
\longrightarrow
\text{恢复完整圆柱}
\longrightarrow
\text{表面采样严格校核}
\longrightarrow
\text{确定最终结果}
}.
\]

最终结果建议为：

| 模型口径 | 有效遮蔽区间 / s | 有效遮蔽时长 / s | 作用 |
|---|---:|---:|---|
| 下底面圆心特征点 | \([8.0379,9.4481]\) | \(1.4102\) | 基础简化模型与程序校验 |
| 完整圆柱全部遮蔽 | \([8.0564,9.4481]\) | \(1.3916\) | 最终严格模型 |

因此，第一问最终取

\[
\boxed{T_{\mathrm{eff}}\approx1.39\,\mathrm{s}}.
\]

---

## 参考文献

[1] 陶梦高, 董文锋, 罗威, 等. 空中烟幕投放控制建模与仿真分析[J]. 空天预警研究学报, 2022, 36(4): 281-285. [在线链接](https://kldx.cbpt.cnki.net/portal/journal/portal/client/paper/c4ef885ad75919d970585a2693db0d70)

[2] 郭爱强, 李天鹏, 李笑楠, 等. 烟幕干扰激光末制导炮弹的仿真研究[J]. 红外与激光工程, 2022, 51(4). DOI: 10.3788/IRLA20210225. [在线链接](https://www.spacejournal.cn/hwyjggc/cn/article/doi/10.3788/IRLA20210225)

[3] 陈柳莹, 李晓霞, 王晓农, 等. 烟幕遮蔽和干扰效果评价方法研究[J]. 激光与光电子学进展, 2023, 60(22): 2200004. DOI: 10.3788/LOP230725. [在线链接](https://www.researching.cn/ArticlePdf/m00002/2023/60/22/2200004.pdf)

[4] 曾照凯, 朱东升, 郭潇迪, 等. 基于红外遮蔽的地面烟幕防空阵地部署模型研究[J]. 兵工学报, 2019, 40(6): 1244-1251. DOI: 10.3969/j.issn.1000-1093.2019.06.015. [在线链接](https://www.co-journal.com/zh/article/doi/10.3969/j.issn.1000-1093.2019.06.015/)
