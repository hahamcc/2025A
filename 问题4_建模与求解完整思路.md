# 问题 4：三架无人机各投一枚烟幕弹——完整建模与分层求解思路

> **当前定案**：问题 4 始终以“三枚烟幕联合完整遮蔽真圆柱”的总时长 \(T_{\rm joint}\) 为正式目标。点目标模型只用于缩小无效搜索量、发现多个优质协同模式和生成初值；最终结论必须由完整圆柱表面联合判据给出。

---

## 一、问题 4 相对于问题 3 改变了什么

问题 3 是 FY1 连续投放三枚烟幕弹：三枚弹共用同一航向和速度，且相邻两次**投放**至少间隔 1 s。

问题 4 则由 FY1、FY2、FY3 各投放一枚烟幕弹。三架无人机可以分别选择自己的航向、速度、投放时刻和引爆延迟。因此，问题 4 的自由度更大：三团烟幕可以从不同位置、不同方向和不同时间参与遮蔽。

但题目没有给出以下限制，主模型不应自行加入：

- 无人机之间的避碰约束；
- 无人机通信延迟；
- 三枚弹必须依次起爆的约束；
- 三枚弹之间必须间隔 1 s 的约束。

“相邻投放至少间隔 1 s”只属于问题 3 的同一架无人机连续投放，不适用于问题 4。

---

## 二、运动模型与正式决策变量

### 1. 三架无人机

记第 \(i\) 架无人机的初始位置为

\[
F_{i0}=(x_{i0},y_{i0},z_{i0}),\qquad i=1,2,3.
\]

其水平航向角和速度分别为 \(\theta_i\)、\(v_i\)。接到任务后以固定高度作匀速直线飞行：

\[
F_i(t)=F_{i0}+v_it(\cos\theta_i,\sin\theta_i,0).
\]

### 2. 第 \(i\) 枚烟幕弹

第 \(i\) 枚弹在 \(t_{ri}\) 投放，经过 \(\tau_i\) 后起爆：

\[
t_{ei}=t_{ri}+\tau_i.
\]

投放至起爆前，烟幕弹继承无人机的水平速度并自由下落；起爆点为

\[
P_{ei}=
\left(
x_{i0}+v_it_{ei}\cos\theta_i,
y_{i0}+v_it_{ei}\sin\theta_i,
z_{i0}-\frac12g\tau_i^2
\right).
\]

起爆后形成半径 \(R=10\) m 的球形烟幕，固定有效 20 s，球心不再水平移动，以 3 m/s 匀速下沉：

\[
C_i(t)=P_{ei}+(0,0,-3(t-t_{ei})),
\qquad t\in[t_{ei},t_{ei}+20].
\]

### 3. 论文中的决策变量

论文中按题意报告 12 个决策变量：

\[
\boldsymbol x=
(\theta_1,v_1,t_{r1},\tau_1,\;
\theta_2,v_2,t_{r2},\tau_2,\;
\theta_3,v_3,t_{r3},\tau_3).
\]

每架无人机独立满足：

\[
0\le\theta_i<2\pi,\qquad70\le v_i\le140,
\]

\[
t_{ri}\ge0,\qquad\tau_i\ge0,
\]

\[
t_{ri}+\tau_i=t_{ei}\le T_{\max,i}.
\]

记导弹 \(x\) 方向速度为

\[
v_{Mx}=300\cdot\frac{20000}{\sqrt{20000^2+2000^2}}
\approx298.511\text{ m/s},
\]

飞行终点时间为

\[
T_M=\frac{\sqrt{20000^2+2000^2}}{300}
\approx66.9992\text{ s}.
\]

若烟幕球心在 \(x\) 方向仍位于导弹前方超过一个烟幕半径，则它不可能接触导弹到真目标的任何视线段。第 \(i\) 架无人机沿 \(-x\) 方向以最大速度飞行时，烟幕球心可达到的最小 \(x\) 坐标为 \(x_{i0}-140t\)。由

\[
x_{i0}-140t\le 20000-v_{Mx}t+R
\]

得到起爆时刻的必要上界：

\[
T_{\max,i}=\min\left\{
\frac{20000+R-x_{i0}}{v_{Mx}-140},\;T_M
\right\}.
\]

代入三架无人机初始 \(x\) 坐标后，正式采用：

\[
t_{e1}\le13.9422\text{ s},\qquad
t_{e2}\le50.5327\text{ s},\qquad
t_{e3}\le66.9992\text{ s}.
\]

FY3 的解析值约为 \(88.3849\) s，超过导弹飞行终点，故截断为 \(T_M\)。

每一枚弹还须满足起爆点不低于地面：

\[
z_{i0}-\frac12g\tau_i^2\ge0,
\qquad
\tau_i\le\sqrt{\frac{2z_{i0}}{g}}.
\]

由三架无人机的初始高度，得到：

\[
\tau_1\le19.1663\text{ s},\qquad
\tau_2\le16.9031\text{ s},\qquad
\tau_3\le11.9523\text{ s}.
\]

全文统一采用题设最简口径：只要求起爆点不低于地面。

---

## 三、用于计算的等价重参数化：先消除无意义组合

直接搜索 \((t_{ri},\tau_i)\) 时，投放时刻与延迟耦合较强。代码中对每架无人机改用：

\[
(\theta_i,s_i,t_{ei},\tau_i),
\qquad s_i=v_it_{ei},
\]

其中 \(s_i\) 为起爆前的水平飞行距离。原变量由

\[
t_{ri}=t_{ei}-\tau_i,
\qquad
v_i=\frac{s_i}{t_{ei}}
\]

恢复。

相应约束为：

\[
0\le\tau_i\le t_{ei}\le T_{\max,i},
\]

\[
70t_{ei}\le s_i\le140t_{ei}.
\]

同时还须满足 \(\tau_i\le\sqrt{2z_{i0}/g}\)。这不是减少变量个数，也不改变题意；它的作用是使每一个候选的“起爆时间—起爆高度—水平起爆位置”关系天然一致，减少优化器产生无物理意义样本的机会。代码中 \(t_{ei}=0\) 的退化边界可单独处理，实际搜索中取一个极小正数即可。

此时起爆点的水平位置可直接写成：

\[
P_{ei,h}=F_{i0,h}+s_i(\cos\theta_i,\sin\theta_i).
\]

即优化器实际上是在决定：每架无人机在什么时候、以多高的自由落体高度、把烟幕送到哪个水平位置。

---

## 四、联合完整遮蔽：问题 4 的唯一正式目标

真目标为半径 7 m、高 10 m 的圆柱。记其外表面为 \(\partial\Omega\)，导弹在时刻 \(t\) 的位置为 \(M(t)\)。对任一表面点 \(Q\in\partial\Omega\)，导弹观察该点的视线是有限线段 \(\overline{M(t)Q}\)。

令 \(\mathcal J(t)\) 表示时刻 \(t\) 仍有效的烟幕集合，定义

\[
g_t(Q)=
\min_{i\in\mathcal J(t)}
d\bigl(C_i(t),\overline{M(t)Q}\bigr).
\]

它表示：对目标点 \(Q\)，三团有效烟幕中选择最适合遮挡它的一团。

再定义最不利目标点的距离：

\[
G(t)=\max_{Q\in\partial\Omega}g_t(Q).
\]

若

\[
G(t)\le R=10,
\]

则圆柱外表面的每一个点至少都被一团烟幕遮住，时刻 \(t\) 记为联合有效遮蔽。

最终目标为所有有效区间的并集长度：

\[
\max\;T_{\rm joint}
=\max\int I\{G(t)\le10\}\,dt.
\]

问题 4 中三团烟幕的并集一般不是凸集。因此，不能像单枚烟幕那样把上下圆周采样当作正式判据；正式搜索与复核均应以完整圆柱表面为准。

---

## 五、几何快速跳过：仅用于减少距离计算

取真目标的几何中心 \(O\)。整个圆柱位于以 \(O\) 为球心、半径

\[
a=\sqrt{7^2+5^2}\approx8.60\text{ m}
\]

的外接球内。对任一表面点 \(Q\)，有 \(\|Q-O\|\le a\)。因此，若当前时刻的第 \(i\) 团有效烟幕满足

\[
d\bigl(C_i(t),\overline{M(t)O}\bigr)>R+a,
\]

则它不可能遮住真目标的任何表面点。程序在该时刻无需计算这团烟幕与完整圆柱采样点的距离。

实现口径为：

```text
对每一团当前有效烟幕 i：
    若 d(C_i(t), 导弹—目标中心视线段) > 10 + a：
        跳过该烟幕的完整表面距离计算
    否则：
        计算该烟幕对完整圆柱表面采样点的距离

若当前有效烟幕均被跳过：
    当前时刻直接判为联合遮蔽无效
```

该判断是评价器内部的早停加速，不构成独立模型，不用来人为限定航向、速度或时间范围。

---

## 六、单弹候选库：只用于构造三机初始种子

为避免 12 维 DE 从完全随机位置出发，先为 FY1、FY2、FY3 分别生成少量单弹可行候选。点目标模型仅在这一环节使用：它用于快速产生候选，不能作为第四问的优化目标或正式结果。

单弹候选库的生成可选三种方式：

| 方法 | 优点 | 局限 |
|---|---|---|
| 单弹点目标粗搜索 | 最快 | 容易偏向目标中心视线 |
| 单弹低密度完整表面搜索 | 与正式目标更接近 | 计算较慢 |
| 点目标产生候选，再以低密度完整表面重排 | 速度与候选质量较平衡 | 多一层轻量重排 |

正式采用第三种。对每架无人机保留若干个低密度完整表面表现较好且彼此分散的单弹策略；将三架无人机的候选作少量组合，形成接力、局部重叠和不同起爆方位的多样化三机初始种子。候选库只改善 DE 起点，不改变联合完整遮蔽 \(T_{\rm joint}\) 这一正式目标。

---

## 七、正式求解流程：沿用问题 3 的“先快后严”框架

### 阶段 A：变量可行化与单弹候选库

1. 对三架无人机分别使用 \((\theta_i,s_i,t_{ei},\tau_i)\) 参数化；
2. 严格满足三架无人机各自的 \(T_{\max,i}\)、速度、非负时间和起爆高度约束；
3. 对每架无人机用点目标快速产生单弹候选，并以低密度完整表面重排；
4. 每架保留若干个分散候选，组合成少量多样化的三机初始种子。

该阶段只改善正式 DE 的起点；不进行 12 维点目标优化，也不预先给协同模式贴标签。

### 阶段 B：12 维完整圆柱联合 DE

将单弹候选组合得到的种子与全域 LHS 可行个体混合，直接启动 12 维 DE。评价器从搜索开始即使用正式的 \(T_{\rm joint}\)。

建议初始种群以单弹组合种子为主，同时保留约 \(20\%\sim30\%\) 的全域 LHS 可行个体，避免候选库的偏好束缚正式搜索。

搜索中使用由低到中等的表面采样密度和时间步长；不使用双圆周作为第三、四问的正式联合判据。

### 工程加速：缓存、早停与并行计算

完整圆柱联合 DE 中，同一代不同个体的适应度函数互不依赖，因此可以并行计算。并行化只改变运行耗时，不改变物理模型、目标函数和最终复核口径。

| 并行方式 | 适合阶段 | 优点 | 注意事项 |
|---|---|---|---|
| 不同随机种子的任务并行 | 完整表面阶段 | 最稳定，便于比较结果 | 每次 DE 内部仍是串行 |
| NumPy 向量化与缓存 | 单弹候选、完整表面阶段 | 无进程通信开销，减少重复距离计算 | 需预生成表面点和导弹轨迹 |
| SciPy `differential_evolution(workers=...)` 或 `multiprocessing` | 单次完整圆柱 DE | 单个适应度较慢时通常收益明显 | 评价器必须可序列化，采样数组复制会占用内存 |

推荐做法是：预生成并缓存圆柱表面点、导弹位置和视线计算所需固定量；在每个时刻先执行第五节的几何跳过；完整表面 DE 再用受控数量的工作进程并行评价种群。Windows 下必须使用：

```python
if __name__ == "__main__":
    main()
```

若 SciPy DE 使用 `workers=-1` 或进程池映射，需采用 `updating="deferred"`；同时应预生成并只读共享圆柱采样点，避免每个个体重复构造表面。进程数不必盲目取满，可保留 1～2 个逻辑核给系统，并在正式运行前比较串行、半核并行和满核并行的实际耗时与内存占用。

### 阶段 C：逐级加密与最终复核

合并各次完整表面 DE 的前若干候选，依次进行：

1. 中密度完整圆柱表面与较小时间步长重排；
2. 高密度完整圆柱表面扫描；
3. 对进入、退出有效遮蔽的边界使用二分法定位；
4. 对冠军方案再作一轮更密的交叉复核。

最终以本阶段的 \(T_{\rm joint}\)、联合遮蔽区间及对应的三机策略作为正式答案。

---

## 八、协同模式只用于结果解释

接力、交叠接力、空间分工和冗余集中之间没有严格边界；同一最优方案可能同时具有多种特征。因此，不将它们作为搜索前的硬分类或多套子问题。

搜索结束后，再根据最终方案的烟幕有效区间、各弹单独遮蔽区间及协同增益

\[
\Delta T=T_{\rm joint}-T_{\rm ind}
\]

进行解释：

| 结果特征 | 推荐解释 |
|---|---|
| 三枚弹的有效遮蔽区间依次衔接、重叠较少 | 以时间接力为主 |
| 相邻区间短暂重叠，以避免出现遮蔽空档 | 交叠接力 |
| \(\Delta T\) 明显为正，且同一时刻不同烟幕遮挡目标的不同区域 | 存在空间分工协同 |
| 多枚弹长期重叠而 \(\Delta T\) 很小 | 冗余集中，协同效率较低 |

这种“先求解、后分类”的叙事更自然，也不会向算法额外引入人为参数。

---

## 九、三种搜索组织方式及选择

| 方案 | 做法 | 优点 | 局限 |
|---|---|---|---|
| 直接 12 维完整表面 DE | 从全域随机种群直接优化 \(T_{\rm joint}\) | 模型最直接 | 计算最慢，初期大量零值或低质量个体 |
| 人为硬缩小航向和时间范围 | 根据直觉限定每架无人机的方向、时段 | 运行最快 | 漏掉角色交换或意外协同方案的风险最大 |
| 单弹候选组合 + 全域 LHS 种子 + 完整表面 DE | 以低成本候选改善初值，正式阶段仍直接优化 \(T_{\rm joint}\) | 效率、简洁性和防漏解能力较均衡 | 候选库生成仍需额外计算 |

正式采用第三种。

论文中应如实说明：LHS 和 DE 是启发式搜索，不能证明理论全局最优；可以表述为“在给定可行域、采样精度、单弹候选种子和多随机种子条件下得到的推荐最优策略”。

---

## 十、结果输出、验证与论文叙事

### 1. 输出内容

按题目 `result2.xlsx` 模板逐行填写 FY1、FY2、FY3 的：

- 航向角和速度；
- 投放时刻、引爆延迟、起爆时刻；
- 投放点和起爆点；
- 该弹单独完整遮蔽的时长（按模板要求填写）。

另外在 CSV、控制台和论文中报告：

- 联合遮蔽区间及 \(T_{\rm joint}\)；
- 三枚烟幕各自的有效时间窗；
- 独立完整遮蔽总时长 \(T_{\rm ind}\)；
- 协同增益 \(\Delta T=T_{\rm joint}-T_{\rm ind}\)。

其中 \(T_{\rm ind}\) 和 \(\Delta T\) 仅用于解释最终策略，不参与主搜索目标。

### 2. 必要验证

- 每架无人机均满足速度、时间和起爆高度约束；
- 三团烟幕只在各自有效窗内参与 \(\min_i\) 运算；
- 三架无人机分别满足 \(t_{ei}\le T_{\max,i}\) 与 \(z_{i0}-\frac12g\tau_i^2\ge0\)；
- 全部最终候选都通过完整圆柱表面复核；
- 逐次加密采样后，冠军方案的时长和区间稳定；
- 多随机种子搜索所得优质结果处于相近水平；
- 结果表、代码和论文使用同一套变量定义与遮蔽判据；
- 不将单弹点目标候选或低精度结果直接当作正式答案；
- 几何快速跳过仅跳过当前时刻不可能贡献的烟幕，不改变完整表面联合判据。

### 3. 推荐论文承接顺序

1. 承接问题 3，指出三架无人机独立机动解除共同航向、共同速度和同机投放间隔限制；
2. 建立三架无人机、三枚烟幕弹的运动方程及 12 个决策变量；
3. 给出联合完整遮蔽判据，并说明多球并集非凸，正式判定需要完整圆柱表面；
4. 推导每架无人机各自的起爆时刻上界与起爆高度约束，并说明变量重参数化；
5. 说明单弹点目标候选经低密度完整表面重排后，只用于构造三机 DE 初始种子；
6. 展示“单弹候选库—三机多样化种子—完整表面联合 DE—高精度复核”的求解流程；
7. 报告最终三机方案、联合遮蔽时间及协同增益；
8. 说明多种子、加密复核和启发式算法的局限性。

整条逻辑可概括为：

\[
\text{三机独立运动模型}
\rightarrow
\text{精确时序/高度约束与单弹候选库}
\rightarrow
\text{三机多样化初始种子}
\rightarrow
\text{完整圆柱联合精搜索}
\rightarrow
\text{高精度严格复核}
\rightarrow
\text{推荐策略与协同模式解释}.
\]
