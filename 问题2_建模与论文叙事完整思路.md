# 问题 2：建模、求解与论文叙事完整思路

> 本文档承接问题一，既说明“为什么这样建模”，也明确“程序怎样求解、论文怎样叙述”。当前先确定模型与流程；最终最优参数必须由程序搜索和高精度复核给出。

## 一、问题二在做什么

一架无人机投放一枚烟幕干扰弹。可以自主决定无人机的飞行方向、飞行速度、投放时刻和引爆延时；目标是让真实圆柱目标的**总有效遮蔽时间最长**。

问题一解决“参数给定时能遮蔽多久”；问题二解决“参数怎样选才能遮蔽最久”。所以问题二不另建遮蔽规则，而是把问题一已经验证的遮蔽判定器反复调用，作为优化的评价函数。

```text
候选控制参数
    ↓
无人机轨迹、投放点、爆炸点、烟幕球轨迹
    ↓
问题一的有限视线段—烟幕球相交判定
    ↓
该方案的有效遮蔽区间与总时长
    ↓
优化算法保留总时长最大的方案
```

## 二、从问题一到问题二的逻辑承接

问题一已经完成以下固定流程：给定无人机的方向、速度、投放时刻和引爆延时，计算烟幕球的位置；再判断导弹至真实圆柱目标边界的每条视线是否与烟幕球相交，输出全部有效区间及总时长。

把这个过程记成

\
T_{\rm eff}=\mathcal E(\theta,v,t_r,\tau).
\

问题二只是将原来固定的四个量变成待选变量，并求

\[
\max T_{\rm eff}.
\]

因此，问题一的运动学、有限线段判据、时间扫描和边界精修全部保留；问题二只在外层增加搜索最优参数的算法。这种模块化写法也为问题三至五留好接口：以后增加的是多枚弹和多架无人机的约束，不是推倒单枚弹评价器重来。

## 三、变量与重参数化

### 3.1 题目中直观的四个变量

设无人机初始位置为 \(F_0=(17800,0,1800)\)。

| 符号 | 含义 | 单位 |
|---|---|---|
| \(\theta\) | 无人机水平飞行方向角；\(+x\) 方向为 0，逆时针为正 | rad 或 ° |
| \(v\) | 无人机速度 | m/s |
| \(t_r\) | 投放时刻 | s |
| \(\tau\) | 投放后至爆炸的延时 | s |

爆炸时刻为 \(t_e=t_r+\tau\)。无人机一旦确定方向和速度，任务过程中保持水平匀速直线飞行。

### 3.2 程序建议使用的变量

直接搜索 \((\theta,v,t_r,\tau)\) 时，需要额外处理很多不合格组合。推荐程序改为搜索

\[
(\theta,v,t_e,q),\qquad q=\frac{\tau}{t_e},\quad 0\le q\le1.
\]

再还原为

\[
\tau=qt_e,\qquad t_r=(1-q)t_e.
\]

这样投放时刻、引爆延时均自动非负，且投放一定不晚于爆炸；最终报告时仍能还原为题目要求的 \(t_r\) 和 \(\tau\)。

## 四、运动学模型

令无人机的水平单位方向为

\[
e(\theta)=(\cos\theta,\sin\theta,0).
\]

无人机轨迹和投放点为

\[
F(t)=F_0+vt\,e(\theta),\qquad P_r=F(t_r).
\]

干扰弹脱离无人机后继承水平速度并受重力作用，爆炸点为

\[
P_e=P_r+v\tau e(\theta)+(0,0,-\tfrac12g\tau^2).
\]

代入 \(t_r+\tau=t_e\)，可化为

\[
P_e=F_0+vt_e e(\theta)+(0,0,-\tfrac12g(qt_e)^2).
\]

这说明爆炸点的水平位置由 \(\theta,v,t_e\) 决定，投放与引爆之间如何分配主要影响爆炸高度。

爆炸后烟幕球不保留水平速度，只以 3 m/s 下沉；未考虑地面时的题设时间窗为：

\[
C(t)=P_e+(0,0,-3(t-t_e)),\qquad t\in[t_e,t_e+20].
\]

导弹 M1 按题意以 300 m/s 指向假目标作匀速直线飞行，直接调用问题一的实现。

### 4.1 爆炸高度与触地约束

爆炸高度为

\[
z_e=1800-\frac12g\tau^2
=1800-\frac12g(qt_e)^2.
\]

这一高度不能只在程序结束时口头检查，而应显式写入可行域。对“地面”的处理有三种口径：

| 口径 | 约束或处理 | 优点 | 局限 |
|---|---|---|---|
| A：严格按题设最简化 | 不额外引入地面，仅用题设 20 s 有效期 | 与题面给定完全一致，最简洁 | 可能出现烟幕球已下沉到地面以下仍被计为有效的物理瑕疵。 |
| B：球心不触地 | 要求 \(z_e-3\times20>0\)，即 \(\tau<\sqrt{2\times1740/g}\approx18.84\) s | 正是“烟幕中心在完整 20 s 内不穿过地面”的明确约束，容易写入论文 | 没有考虑半径 10 m 的球体下缘会先触地。 |
| C：整球不触地 | 要求 \(z_e-3\times20-R>0\)，即 \(\tau<\sqrt{2(1800-60-10)/g}\approx18.79\) s | 若把烟幕视为实体球，这是最严格的 20 s 全程物理约束 | 比题设更保守，可能删去“前一段本可有效”的方案。 |

本文主模型采用一个兼顾物理性和不丢失可行解的处理：爆炸时要求 \(z_e>R\)，烟幕球下缘首次接触地面的时刻为

\[
t_g=t_e+\frac{z_e-R}{3},
\]

因此实际可用时间窗取为

\[
\left[t_e,\ t_e+\min\left(20,\frac{z_e-R}{3}\right)\right].
\]

也就是说，触地后不再计入遮蔽，触地前仍保留其本来可以产生的效果。为展示方案在更严格物理假设下是否仍然成立，可将口径 B 或 C 作为鲁棒性情景复算。当前初始搜索采用 \(t_e\le14\) s，必有 \(\tau\le14\) s，故上述三个 20 s 全程约束均不会成为活跃边界；但显式写出它们能防止后续放大搜索范围时出现“钻地爆炸”。

## 五、目标函数、遮蔽判据与约束

对圆柱目标边界点 \(Q\)，在时刻 \(t\) 考察从导弹 \(M(t)\) 到 \(Q\) 的有限线段

\[
X(s)=M(t)+s[Q-M(t)],\quad 0\le s\le1.
\]

若该线段与烟幕球相交，则该点被遮挡；若全部目标边界采样点均被遮挡，则该时刻有效。导弹已处在烟幕球内时，视线从烟幕内部出发，按这一有限线段判据仍属于遮挡，不应额外排除。

记有效时刻集合为 \(\mathcal I\)，目标函数为

\[
\max_{\theta,v,t_e,q} T_{\rm eff}
=\max_{\theta,v,t_e,q}\operatorname{meas}(\mathcal I).
\]

建议使用的初始范围如下。它们是用于节省计算的物理筛选，必须在程序中验证，而不是直接当成结论。

| 变量 | 初始范围 | 理由 |
|---|---:|---|
| \(\theta\) | 先在 \([0,2\pi)\) 低成本试探；正式搜索暂用 \([\pi/2,\pi]\) | 烟幕需要总体向左、向真实目标一侧移动。正式缩小范围前，先确认全方向优质候选确实落在该扇区。 |
| \(v\) | \([70,140]\) m/s | 题设范围。 |
| \(t_e\) | \([0.1,14]\) s | 导弹向左的速度约 298.5 m/s，最快无人机向左仅 140 m/s；爆炸过晚时烟幕很难仍处于导弹和目标之间。若上界附近出现好解，必须扩大上界复查。 |
| \(q\) | \([0,1]\) | 保证 \(t_r\ge0,\tau\ge0\)。 |

程序还应检查 \(z_e>R\)、触地截断后的烟幕有效期与导弹到达假目标前的时间有交集，以及每个变量均未超题设范围。

这里的“烟幕有效期”应采用第 4.1 节的触地截断时间窗，而不再机械写为 \([t_e,t_e+20]\)。

### 5.1 零遮蔽平原的引导量

直接以 \(T_{\rm eff}\) 为 DE 适应度时，很多随机方案可能都得到 0 s，种群没有足够的优劣信息。为避免这一情况，设 \(\mathcal Q_N\) 为当前评价器的目标采样点集合，定义最不利视线到烟幕球心的距离

\[
D_N(t;x)=\max_{Q\in\mathcal Q_N}\ 
\min_{0\le s\le1}
\left\|C(t;x)-\big[M(t)+s(Q-M(t))\big]\right\|.
\]

其中 \(x=(\theta,v,t_e,q)\)。只有 \(D_N(t;x)\le R\) 时，所有采样目标点才同时被遮挡。这一定义必须取“所有视线中的最大距离”；只取某一条最近视线，会把“挡住一点点目标”误认为接近完全遮蔽。

对完全没有有效遮蔽的方案，定义到可行区的间隙

\[
\Phi(x)=\max\left\{0,\min_t[D_N(t;x)-R]\right\}.
\]

\(\Phi\) 越小，说明该方案越接近在某时刻完全遮蔽整个目标。

### 5.2 三种应对零适应度平原的办法

| 方法 | 做法 | 优点 | 局限 |
|---|---|---|---|
| A：直接最大化 \(T_{\rm eff}\) | 保持原目标函数，不增加引导 | 最简单，结果含义最直接 | 若初始种群大量为 0，DE 可能退化为近似随机游走。 |
| B：单阶段分级适应度 | 最小化 \(f(x)=-T_{\rm eff}(x)\)（有遮蔽时）；无遮蔽时最小化 \(f(x)=\Phi(x)\) | 所有有正时长方案的适应度为负，天然优于无解方案；实现改动较小 | \(\Phi\) 仍含有 max/min，不是严格光滑函数，只是信息更丰富。 |
| C：两阶段搜索 | 第一阶段最小化 \(\Phi\) 找到可行遮蔽区；第二阶段以这些候选为种子最大化 \(T_{\rm eff}\) | 分工清楚，不必把距离单位与时间单位硬拼为一个加权目标；最稳妥 | 需多一次短搜索，计算量略增。 |

本文推荐 C 作为“零值比例较高”时的正式备选：先用低密度双圆周最小化 \(\Phi\)，保留 \(\Phi\) 最小的若干方案；再以这些方案和随机种群共同初始化 DE，最大化 \(T_{\rm eff}\)。若全方向试探已经发现稳定的正时长样本，则可直接采用 A/B，避免不必要的额外计算。

## 六、三种求解方案的比较

| 方案 | 做法 | 优点 | 局限 |
|---|---|---|---|
| A：四维网格搜索 | 四个变量逐点枚举 | 直观，便于画出局部规律与检查边界 | 即使每维 50 点也有 625 万组，配合高精度遮蔽判定计算量过大。 |
| B：差分进化（DE） | 种群经变异、交叉、筛选不断寻找更优点 | 不要求目标函数可导，适合遮蔽时长这种有阈值、可能不光滑的黑箱函数；全局探索能力较好 | 无严格全局最优保证；需要多随机种子复现。 |
| C：多起点局部搜索 | 从多个较好初值附近细调 | 后期收敛快，适合精修 | 单独使用易陷局部最优，且本题目标函数存在状态跳变。 |

### 采用的组合策略

采用“**全方向粗验 + DE 主搜索 + 高精度复核**”。先防止物理筛选漏掉大方向；再用 DE 找多个好解；最后以高精度判据重算。这比一次随机运行后直接报答案更可靠。

## 七、分层评价器：让计算量可控

问题一已验证完整圆柱外表面与上、下双圆周在本题下给出相同结果，但点数相差很大。

| 评价器 | 当前每时刻目标点数 | 用途 |
|---|---:|---|
| 完整圆柱外表面 | 148320 | 问题一正式结果、最终候选的严格复核 |
| 双圆周（每圈 1440 点） | 2880 | 已验证的快速精算版本 |
| 双圆周低密度版 | 每圈 90、180 或 360 点 | 优化中的粗筛与主搜索 |

建议采用以下精度层次：

| 阶段 | 每圈点数 | 时间步长 | 作用 |
|---|---:|---:|---|
| I：范围试探 | 90 | 0.10 s | 验证方向与爆炸时刻范围 |
| II：DE 主搜索 | 180 或 360 | 0.05 s | 找到前若干候选 |
| III：候选重排序 | 1440 | 0.02–0.05 s，端点二分 | 稳定比较前 10 名左右 |
| IV：最终确认 | 完整表面 | 0.05 s 扫描 + 端点二分 | 给出论文最终值 |

低精度只负责把搜索引向好区域，绝不直接作为最终结论。

## 八、DE 的实施流程

对 \(x=(\theta,v,t_e,q)\)：

1. 还原 \(t_r=(1-q)t_e\)、\(\tau=qt_e\)；
2. 计算爆炸点和烟幕球轨迹；
3. 调用问题一双圆周评价器，找出有效区间；
4. 返回总时长 \(T_{\rm eff}\)。

多数优化库默认最小化，因此传入函数可写为 \(f(x)=-T_{\rm eff}(x)\)。

初次运行可采用：

| 阶段 | DE 种群规模 | 最大迭代数 | 随机种子数 | 评价器 |
|---|---:|---:|---:|---|
| 全方向试探 | `popsize=12` | 80 | 3 | 每圈 90 点，\(\Delta t=0.10\) s |
| 正式搜索 | `popsize=20` | 150 | 至少 3 | 每圈 180 或 360 点，\(\Delta t=0.05\) s |
| 最终复核 | 不再运行 DE | — | — | 高密度双圆周和完整表面 |

伪代码：

```text
全方向低成本试探
若优质候选均位于 [90°,180°]，保留该正式范围；否则扩大正式范围

for 至少 3 个随机种子：
    DE 最小化 -T_eff(theta,v,te,q)
    保存每次前 10 个候选，而不是只保留第一名

合并、去重候选
高密度双圆周重算并排序
前 3～5 名用完整圆柱外表面复算
输出最优参数、投放点、爆炸点、有效区间和总时长
```

## 九、结果出来后要做的灵敏度、鲁棒性与验证

1. **多种子一致性**：至少三次 DE 的好解是否落在相近区域；
2. **精度一致性**：低密度圆周、高密度圆周和完整表面的结果是否稳定；
3. **单因素灵敏度（OAT）**：固定其余变量，对 \(v,t_r,\tau\) 分别施加 \(\pm1\%,\pm3\%,\pm5\%\) 扰动；对航向角采用 \(\pm1^\circ,\pm3^\circ,\pm5^\circ\) 的绝对扰动。若某个时刻变量接近 0，则改用小的绝对秒数扰动，避免“百分比几乎为零”失去意义。每次扰动均需截断到可行域，并画出“扰动量—遮蔽时长”曲线；
4. **随机鲁棒性检验**：在最优方案周围按预设控制误差随机生成一批样本，例如航向标准差 \(1^\circ\)、速度和时间标准差为标称值的 1%。报告遮蔽时长的均值、分位数和仍保持正遮蔽的比例，补充 OAT 无法显示的变量耦合影响；
5. **严格情景检验**：分别采用球心不触地（\(\tau<18.84\) s）和整球不触地（\(\tau<18.79\) s）口径复算，说明地面假设是否影响最优结论；
6. **边界与物理检查**：检查速度、方向、延时比例、爆炸时刻的边界；核查爆炸高度、触地截断时间、烟幕有效窗、导弹位置与遮蔽区间是否匹配。

三种鲁棒性分析方式也可比较：OAT 最容易解释、适合正文主图；随机扰动能反映变量同时误差、适合补充表；最坏情景优化能直接追求保底性能，但会把题设的“最大时长”目标改成另一类问题，建议只作为扩展讨论，不替代主结果。

## 十、论文叙事如何写

### 10.1 问题二开头的承接段

> 问题一已建立给定投放参数下的单枚烟幕弹遮蔽评价模型：在每一时刻，考察导弹至真实圆柱目标边界各点的有限视线段是否与烟幕球相交，并由此得到有效遮蔽区间。问题二中，无人机的航向、速度、投放时刻和引爆延时均可自主决策。因此，本文保持问题一的运动学模型和遮蔽判据不变，将其封装为单枚干扰弹的效能评价器，并在可行控制范围内最大化总有效遮蔽时间。

### 10.2 正文推荐顺序

1. 写清四个实际变量 \((\theta,v,t_r,\tau)\)；
2. 说明程序采用 \((\theta,v,t_e,q)\) 重参数化及还原方法；
3. 写无人机、弹体、烟幕球、导弹的运动方程；
4. 明确继承问题一有限线段—球体相交判据；
5. 给目标函数、范围和可行性约束；
6. 比较网格、DE、多起点局部搜索三种方法，再选择分层 DE；
7. 给出流程图/伪代码、最优表格和验证结果。

### 10.3 算法选择段落

> 有效遮蔽时间由若干视线遮挡状态的切换时刻决定，目标函数可能含有折点和局部峰值，难以直接使用依赖梯度的连续优化方法。四维均匀网格搜索虽直观，但在高精度遮蔽判定下计算量过大。综合全局探索能力与计算代价，本文采用差分进化算法进行主要搜索；同时采用“低密度筛选—高密度重排序—完整表面复核”的分层评价策略，以兼顾搜索效率和结果可靠性。

结果表建议包含：航向角、速度、投放时刻、引爆延时、爆炸点、有效遮蔽区间、总时长。

### 10.4 问题二正文图表的明确规划

建议正文至少放以下三张图，分别服务于“算法可信”“空间合理”“遮蔽判据可见”三个问题：

1. **DE 收敛曲线图**：横轴为迭代代数，纵轴为截至该代的最佳遮蔽时长；叠加至少 3 个随机种子的曲线。它用于展示不是某一次随机运行偶然得到结果；
2. **最优方案轨迹与遮挡示意图**：用 \(xy\) 平面展示无人机水平轨迹、导弹轨迹、真实/假目标和爆炸点；再用 \(xz\) 投影展示干扰弹抛物线段、烟幕下沉段和高度关系。相比难以阅读的单张 3D 图，两个正交投影更清楚；如篇幅允许，可在附录补充 3D 图；
3. **最不利视线距离曲线**：绘制最优方案下 \(D_N(t)\) 与时间的关系，并画出 \(R=10\) m 的水平阈值线。曲线低于阈值的连续时间段，应与评价器报告的有效遮蔽区间一致；它直接展示“遮住全部目标”而非只遮住中心点。

灵敏度曲线、不同地面口径的对比表和随机扰动结果可放正文末尾或附录，以免主线图表过多。

## 十一、向问题三、四的接口

问题二的输出应保存为一条“单枚弹方案”，至少含无人机编号、\(\theta,v,t_r,\tau,t_e\)、投放点、爆炸点、烟幕有效时间窗、有效区间列表和总时长。

到问题三，只在此基础上增加同一无人机固定航向/速度、相邻投放至少间隔 1 s 等约束；到问题四再增加多无人机协同。

多枚弹阶段的当前工作假设是：每枚弹先按单弹完全遮蔽判据给出区间，再取区间并集且重叠不重复计时。若要允许不同烟幕弹分别遮住目标不同部位而共同构成完整遮蔽，必须另建多球联合遮挡判据，不能直接混用。

## 十二、目前已确定与待验证的内容

### 已确定

- 问题二直接使用问题一的评价器；
- 程序变量采用 \((\theta,v,t_e,q)\)，最后还原题目变量；
- 采用“全方向粗验 + 多次 DE + 高精度复核”；
- 双圆周用于搜索，完整圆柱外表面用于最终结果；
- 遮挡锥只作为问题一判据的极限几何解释，不另设独立物理模型。
- 主模型加入“整球下缘触地即截断”的烟幕有效时间窗；\(18.84\) s（球心）和 \(18.79\) s（整球）作为更严格的 20 s 全程不触地情景约束。
- 当直接 DE 出现大量零遮蔽样本时，采用 \(\Phi\) 引导的两阶段搜索；最终目标仍是最大化真实的 \(T_{\rm eff}\)，不把引导量当作最终成绩。
- 最终结果将通过 OAT 灵敏度、随机扰动和严格地面情景复核，并按收敛图、轨迹图、\(D_N(t)\) 曲线展示。

### 待程序确认

- 全方向粗扫是否支持将正式航向限制在 \([90^\circ,180^\circ]\)；
- \(t_e\le14\) s 是否足够，需检查上界附近候选；
- 最优参数、最优时长和多种子稳定性；
- 各层评价器实际耗时，据此再微调 DE 参数。
