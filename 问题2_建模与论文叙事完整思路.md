# 问题 2：建模、求解与论文叙事完整思路

> 本文档承接问题一，既说明“为什么这样建模”，也明确“程序怎样求解、论文怎样叙述”。当前先确定模型与流程；最终最优参数必须由程序搜索和高精度复核给出。

## 一、问题二在做什么

一架无人机投放一枚烟幕干扰弹。可以自主决定无人机的飞行方向、飞行速度、投放时刻和引爆延时；目标是让真实圆柱目标的**总有效遮蔽时间最长**。

问题一解决“参数给定时能遮蔽多久”；问题二解决“参数怎样选才能遮蔽最久”。所以问题二不另建遮蔽规则，而是把问题一已经验证的遮蔽判定器反复调用，作为优化的评价函数。

```text
候选控制参数
    ↓
无人机轨迹、投放点、爆炸点、烟幕球轨迹
    ↓
问题一的有限视线段—烟幕球相交判定
    ↓
该方案的有效遮蔽区间与总时长
    ↓
优化算法保留总时长最大的方案
```

## 二、从问题一到问题二的逻辑承接

问题一已经完成以下固定流程：给定无人机的方向、速度、投放时刻和引爆延时，计算烟幕球的位置；再判断导弹至真实圆柱目标边界的每条视线是否与烟幕球相交，输出全部有效区间及总时长。

把这个过程记成

\
T_{\rm eff}=\mathcal E(\theta,v,t_r,\tau).
\

问题二只是将原来固定的四个量变成待选变量，并求

\[
\max T_{\rm eff}.
\]

因此，问题一的运动学、有限线段判据、时间扫描和边界精修全部保留；问题二只在外层增加搜索最优参数的算法。这种模块化写法也为问题三至五留好接口：以后增加的是多枚弹和多架无人机的约束，不是推倒单枚弹评价器重来。

## 三、决策变量与约束

### 决策变量

设无人机初始位置为 \(F_0=(17800,0,1800)\)。

| 符号 | 含义 | 单位 |
|---|---|---|
| \(\theta\) | 无人机水平飞行方向角；\(+x\) 方向为 0，逆时针为正 | rad 或 ° |
| \(v\) | 无人机速度 | m/s |
| \(t_r\) | 投放时刻 | s |
| \(\tau\) | 投放后至爆炸的延时 | s |

爆炸时刻为 \(t_e=t_r+\tau\)。无人机一旦确定方向和速度，任务过程中保持水平匀速直线飞行。

决策向量直接写为

\[
x=(\theta,v,t_r,\tau).
\]

其中爆炸时刻 \(t_e=t_r+\tau\) 是由决策变量导出的量，而不是另设的决策变量。除题设速度范围外，主要的时序约束为

\[
t_r\ge0,\qquad \tau\ge0,\qquad t_r+\tau\le13.94.
\]

最后一条来自第 5.1 节的解析排除证明。它同时保证投放不晚于爆炸，不需要额外引入比例变量。

## 四、运动学模型

令无人机的水平单位方向为

\[
e(\theta)=(\cos\theta,\sin\theta,0).
\]

无人机轨迹和投放点为

\[
F(t)=F_0+vt\,e(\theta),\qquad P_r=F(t_r).
\]

干扰弹脱离无人机后继承水平速度并受重力作用，爆炸点为

\[
P_e=P_r+v\tau e(\theta)+(0,0,-\tfrac12g\tau^2).
\]

也可把爆炸点直接写为

\[
P_e=F_0+v(t_r+\tau)e(\theta)+(0,0,-\tfrac12g\tau^2).
\]

这说明爆炸点的水平位置由 \(\theta,v,t_r+\tau\) 决定，投放与引爆之间如何分配主要影响爆炸高度。

爆炸后烟幕球不保留水平速度，只以 3 m/s 下沉；未考虑地面时的题设时间窗为：

\[
C(t)=P_e+(0,0,-3(t-t_e)),\qquad t\in[t_e,t_e+20].
\]

导弹 M1 按题意以 300 m/s 指向假目标作匀速直线飞行，直接调用问题一的实现。

### 4.1 爆炸高度校核

爆炸高度为

\[
z_e=1800-\frac12g\tau^2.
\]

本题只要求干扰弹在地面以上起爆，即 \(z_e\ge0\)。题目已直接规定云团在起爆后 20 s 内有效，未规定云团触地后失效，因此主模型不自行加入球心触地、整球触地或下缘触地截断。

实际上，由 \(t_r+\tau\le13.94\) 及 \(t_r\ge0\) 可得 \(\tau\le13.94\)，进而 \(z_e\ge847.8\) m，爆炸高度非负自动满足。故 \(z_e\ge0\) 作为物理校核报告，不额外增加一个活跃约束。

## 五、目标函数、遮蔽判据与约束

对圆柱目标边界点 \(Q\)，在时刻 \(t\) 考察从导弹 \(M(t)\) 到 \(Q\) 的有限线段

\[
X(s)=M(t)+s[Q-M(t)],\quad 0\le s\le1.
\]

若该线段与烟幕球相交，则该点被遮挡；若全部目标边界采样点均被遮挡，则该时刻有效。导弹已处在烟幕球内时，视线从烟幕内部出发，按这一有限线段判据仍属于遮挡，不应额外排除。

记有效时刻集合为 \(\mathcal I\)，目标函数为

\[
\max_{\theta,v,t_r,\tau} T_{\rm eff}
=\max_{\theta,v,t_r,\tau}\operatorname{meas}(\mathcal I).
\]

建议使用的初始范围如下。它们是用于节省计算的物理筛选，必须在程序中验证，而不是直接当成结论。

| 变量 | 初始范围 | 理由 |
|---|---:|---|
| \(\theta\) | 先在 \([0,2\pi)\) 低成本试探；仅在验证后才可正式用 \([\pi/2,\pi]\) | 向左、向真实目标一侧移动在物理上更有利，但这不是硬约束；正式缩小范围前，必须确认全方向优质候选确实落在该扇区。 |
| \(v\) | \([70,140]\) m/s | 题设范围。 |
| \(t_r\) | \([0,13.94]\) s | 投放时刻非负；其实际可取上界还受 \(t_r+\tau\le13.94\) 限制。 |
| \(\tau\) | \([0,13.94]\) s | 引爆延时非负；其实际可取上界还受 \(t_r+\tau\le13.94\) 限制。 |
| 联合时序约束 | \(t_r+\tau\le13.94\) s | 由第 5.1 节解析证明给出，是本问唯一新增的耦合约束。 |

程序还应检查 \(z_e\ge0\)、烟幕题设有效期 \([t_e,t_e+20]\) 与导弹到达假目标前的时间有交集，以及每个变量均未超题设范围。

### 5.1 搜索范围缩小的证据层级

搜索范围不能因为“看起来不太可能”就直接裁掉。本文将范围分为三类：

| 类别 | 例子 | 处理原则 |
|---|---|---|
| 题设硬约束 | \(70\le v\le140\)、\(t_r\ge0\)、\(\tau\ge0\) | 可直接作为优化边界。 |
| 解析排除范围 | 爆炸时刻过晚，烟幕球已完全落在导弹后方 | 给出不等式证明后，可直接剔除。 |
| 经验筛选范围 | \(\theta\in[90^\circ,180^\circ]\) | 只能先做全范围低精度试探；经结果验证后才缩小正式搜索域。 |

下面给出 \(t_e\) 上界的解析证明。导弹 M1 的 \(x\) 坐标为

\[
x_M(t)=20000-\frac{300\times20000}{\sqrt{20000^2+2000^2}}t
\approx20000-298.511t.
\]

在任意航向、任意允许速度下，烟幕爆炸点的 \(x\) 坐标最小只能达到“以 140 m/s 完全沿 \(-x\) 飞行”的情形：

\[
x_C(t_e)\ge17800-140t_e.
\]

若爆炸瞬间烟幕球的最左端仍在导弹右侧，即

\[
x_C(t_e)-R>x_M(t_e),
\]

则烟幕球整体位于所有“导弹—真实目标”视线线段的后方。爆炸后烟幕不再水平移动，而导弹继续向 \(-x\) 方向运动，上述位置关系只会更明显，因此之后也不可能形成遮蔽。代入 \(R=10\) m 得

\[
17800-140t_e-10>20000-298.511t_e,
\]

\[
t_e>\frac{2210}{298.511-140}\approx13.94\ \text{s}.
\]

故 \(t_e>13.94\) s 的方案可由解析直接排除。这里的判断利用的是 \(x\) 坐标的必要条件，不依赖对航向的主观猜测。

航向范围则不同。虽然 \(\theta\in[90^\circ,180^\circ]\) 在直觉上会使无人机向左、向真实目标所在的 \(+y\) 侧移动，但全域计算已经找到 \(5.93^\circ\) 附近的更优方案。因此，正式模型必须保留 \(\theta\in[0,2\pi)\)，不得将航向预先限入 \([90^\circ,180^\circ]\)。

## 六、三种求解方案的比较

| 方案 | 做法 | 优点 | 局限 |
|---|---|---|---|
| A：四维网格搜索 | 四个变量逐点枚举 | 直观，便于画出局部规律与检查边界 | 即使每维 50 点也有 625 万组，配合高精度遮蔽判定计算量过大。 |
| B：差分进化（DE） | 种群经变异、交叉、筛选不断寻找更优点 | 不要求目标函数可导，适合遮蔽时长这种有阈值、可能不光滑的黑箱函数；全局探索能力较好 | 无严格全局最优保证；需要多随机种子复现。 |
| C：多起点局部搜索 | 从多个较好初值附近细调 | 后期收敛快，适合精修 | 单独使用易陷局部最优，且本题目标函数存在状态跳变。 |

### 采用的组合策略

采用“**全域 LHS 粗搜索 + 多区域定向 DE + 高精度复核**”。先在完整可行域中均匀抽样，识别多个相互分散的优质区域；再分别从这些区域启动 DE；最后以高精度判据重算。这比一次随机运行后直接报答案更可靠，也不改变正式搜索边界。

## 七、分层评价器：让计算量可控

问题一已验证完整圆柱外表面与上、下双圆周在本题下给出相同结果，但点数相差很大。

| 评价器 | 当前每时刻目标点数 | 用途 |
|---|---:|---|
| 完整圆柱外表面 | 148320 | 问题一正式结果、最终候选的严格复核 |
| 双圆周（每圈 1440 点） | 2880 | 已验证的快速精算版本 |
| 双圆周低密度版 | 每圈 90、180 或 360 点 | 优化中的粗筛与主搜索 |

建议采用以下精度层次：

| 阶段 | 每圈点数 | 时间步长 | 作用 |
|---|---:|---:|---|
| I：全域粗搜索 | 90 | 0.10 s | 8000 点 LHS，识别多个优质区域 |
| II：定向 DE | 180 | 0.05 s | 从 6 个区域分别精搜索，边界仍为完整可行域 |
| III：候选重排序 | 1440 | 0.05 s，端点二分 | 稳定比较前 20 名 |
| IV：最终确认 | 完整表面 | 0.05 s 扫描 + 端点二分 | 复核前 10 名并给出论文最终值 |

低精度只负责把搜索引向好区域，绝不直接作为最终结论。

## 八、DE 的实施流程

对 \(x=(\theta,v,t_r,\tau)\)：

1. 检查 \(t_r+\tau\le13.94\)；
2. 计算爆炸时刻、爆炸点和烟幕球轨迹；
3. 调用问题一双圆周评价器，找出有效区间；
4. 返回总时长 \(T_{\rm eff}\)。

多数优化库默认最小化，因此传入函数可写为 \(f(x)=-T_{\rm eff}(x)\)。

正式运行采用：

| 阶段 | DE 种群规模 | 最大迭代数 | 随机种子数 | 评价器 |
|---|---:|---:|---:|---|
| 全域粗搜索 | — | — | 固定种子 `20250818` | 8000 点 LHS，每圈 90 点，\(\Delta t=0.10\) s |
| 正式搜索 | `popsize=20` | 150 | 6 个区域、6 个固定种子 | 每圈 180 点，\(\Delta t=0.05\) s |
| 最终复核 | 不再运行 DE | — | — | 高密度双圆周和完整表面 |

伪代码：

```text
在完整四维可行域中生成 8000 个 LHS 样本
用低密度双圆周计算每个样本的 T_eff
优先从 T_eff≥3 s 的样本中选取 6 个高质量且分散的区域中心

for 6 个区域中心分别启动一次 DE：
    初始种群 = 32 个中心邻域样本 + 12 个跨区域样本
               + 4 个问题一基线样本 + 32 个全域 LHS 样本
    DE 在完整边界内最小化 -T_eff(theta,v,tr,tau)
    保存每次最终种群前 10 个候选

合并区域中心与 DE 候选并去重
高密度双圆周重算前 20 名并排序
前 10 名用完整圆柱外表面复算
输出最优参数、投放点、爆炸点、有效区间和总时长
```

## 九、结果出来后要做的灵敏度、鲁棒性与验证

1. **多种子一致性**：至少三次 DE 的好解是否落在相近区域；
2. **精度一致性**：低密度圆周、高密度圆周和完整表面的结果是否稳定；
3. **单因素灵敏度（OAT）**：固定其余变量，对 \(v,t_r,\tau\) 分别施加 \(\pm1\%,\pm3\%,\pm5\%\) 扰动；对航向角采用 \(\pm1^\circ,\pm3^\circ,\pm5^\circ\) 的绝对扰动。若某个时刻变量接近 0，则改用小的绝对秒数扰动，避免“百分比几乎为零”失去意义。每次扰动均需截断到可行域，并画出“扰动量—遮蔽时长”曲线；
4. **随机鲁棒性检验**：在最优方案周围按预设控制误差随机生成一批样本，例如航向标准差 \(1^\circ\)、速度和时间标准差为标称值的 1%。报告遮蔽时长的均值、分位数和仍保持正遮蔽的比例，补充 OAT 无法显示的变量耦合影响；
5. **边界与物理检查**：检查速度、方向、投放时刻、引爆延时及 \(t_r+\tau=13.94\) s 边界附近的方案；核查爆炸高度、题设 20 s 烟幕有效窗、导弹位置与遮蔽区间是否匹配。

三种鲁棒性分析方式也可比较：OAT 最容易解释、适合正文主图；随机扰动能反映变量同时误差、适合补充表；边界检查最适合证明搜索范围没有人为截掉优解。

## 十、论文叙事如何写

### 10.1 问题二开头的承接段

> 问题一已建立给定投放参数下的单枚烟幕弹遮蔽评价模型：在每一时刻，考察导弹至真实圆柱目标边界各点的有限视线段是否与烟幕球相交，并由此得到有效遮蔽区间。问题二中，无人机的航向、速度、投放时刻和引爆延时均可自主决策。因此，本文保持问题一的运动学模型和遮蔽判据不变，将其封装为单枚干扰弹的效能评价器，并在可行控制范围内最大化总有效遮蔽时间。

### 10.2 正文推荐顺序

1. 写清四个实际变量 \((\theta,v,t_r,\tau)\)；
2. 给出 \(t_r\ge0\)、\(\tau\ge0\)、\(t_r+\tau\le13.94\) 的可行性约束；
3. 写无人机、弹体、烟幕球、导弹的运动方程；
4. 明确继承问题一有限线段—球体相交判据；
5. 给目标函数、范围和可行性约束；
6. 比较网格、DE、多起点局部搜索三种方法，再选择分层 DE；
7. 给出流程图/伪代码、最优表格和验证结果。

### 10.3 算法选择段落

> 有效遮蔽时间由若干视线遮挡状态的切换时刻决定，目标函数可能含有折点和局部峰值，难以直接使用依赖梯度的连续优化方法。四维均匀网格搜索虽直观，但在高精度遮蔽判定下计算量过大。综合全局探索能力与计算代价，本文采用差分进化算法进行主要搜索；同时采用“低密度筛选—高密度重排序—完整表面复核”的分层评价策略，以兼顾搜索效率和结果可靠性。

结果表建议包含：航向角、速度、投放时刻、引爆延时、爆炸点、有效遮蔽区间、总时长。

### 10.4 问题二正文图表的明确规划

建议正文至少放以下三张图，分别服务于“算法可信”“空间合理”“遮蔽判据可见”三个问题：

1. **DE 收敛曲线图**：横轴为迭代代数，纵轴为截至该代的最佳遮蔽时长；叠加至少 3 个随机种子的曲线。它用于展示不是某一次随机运行偶然得到结果；
2. **最优方案轨迹与遮挡示意图**：用 \(xy\) 平面展示无人机水平轨迹、导弹轨迹、真实/假目标和爆炸点；再用 \(xz\) 投影展示干扰弹抛物线段、烟幕下沉段和高度关系。相比难以阅读的单张 3D 图，两个正交投影更清楚；如篇幅允许，可在附录补充 3D 图；
3. **遮蔽状态时间轴图**：同时画出烟幕题设有效窗、评价器判定的有效遮蔽区间与导弹到达假目标时刻；它直观显示遮蔽时段及其边界来源，不额外引入新的引导函数。

灵敏度曲线和随机扰动结果可放正文末尾或附录，以免主线图表过多。

## 十一、向问题三、四的接口

问题二的输出应保存为一条“单枚弹方案”，至少含无人机编号、\(\theta,v,t_r,\tau,t_e\)、投放点、爆炸点、烟幕有效时间窗、有效区间列表和总时长。

到问题三，只在此基础上增加同一无人机固定航向/速度、相邻投放至少间隔 1 s 等约束；到问题四再增加多无人机协同。

多枚弹阶段的当前工作假设是：每枚弹先按单弹完全遮蔽判据给出区间，再取区间并集且重叠不重复计时。若要允许不同烟幕弹分别遮住目标不同部位而共同构成完整遮蔽，必须另建多球联合遮挡判据，不能直接混用。

## 十二、目前已确定与待验证的内容

### 已确定

- 问题二直接使用问题一的评价器；
- 程序与论文均直接采用 \((\theta,v,t_r,\tau)\)，并以 \(t_r+\tau\le13.94\) s 保证时序可行；
- 采用“全域 LHS 粗搜索 + 六区域 DE + 高精度复核”；
- 双圆周用于搜索，完整圆柱外表面用于最终结果；
- 遮挡锥只作为问题一判据的极限几何解释，不另设独立物理模型。
- 题目未规定烟幕触地失效，故主模型严格采用题设的 20 s 有效期；仅校核干扰弹在地面以上起爆。
- 正式 DE 直接优化 \(-T_{\rm eff}\)，不在主模型中加入额外的零适应度引导函数；若试运行全为零解，优先改善初始种群和粗搜索，而非改变目标函数。
- 最终结果将通过 OAT 灵敏度、随机扰动和边界检查复核，并按收敛图、轨迹图、遮蔽状态时间轴图展示。

### 待程序确认

- 全域粗搜索和正式 DE 均不限制航向；最优方案已落在约 \(5.93^\circ\)，反证 \([90^\circ,180^\circ]\) 预限制不成立；
- \(t_r+\tau\le13.94\) s 的上界附近是否符合解析排除结论；
- 最优参数、最优时长和多种子稳定性；
- 各层评价器实际耗时，据此再微调 DE 参数。

## 十三、主体代码实施记录（不含可视化与鲁棒性分析）

已实现共享评价内核 `core/smoke_evaluator.py`，问题一入口与问题二求解器均调用该内核。问题一固定方案在重构后仍精确复现完整表面和双圆周的正式时长 \(1.3916426693\) s。

问题二标准档先在完整四维可行域中执行 8000 点 LHS 粗搜索（每圈 90 点、\(\Delta t=0.10\) s）。其中 14 个样本形成正遮蔽、7 个样本的遮蔽时长不少于 3 s；从中选出 6 个相互分散的区域中心。随后以每圈 180 点、\(\Delta t=0.05\) s、`popsize=20`、`maxiter=150`，在 6 个区域中心分别启动定向 DE。每次的 80 个初始个体由中心邻域、跨区域候选、问题一基线和全域 LHS 样本共同构成；正式目标仍仅为 \(-T_{\rm eff}\)，正式边界始终是完整可行域。

六次 DE 的最优值介于 \(4.58553\) s 与 \(4.58743\) s 之间。合并候选后，对前 20 名用每圈 1440 点双圆周重排，再对前 10 名用完整圆柱外表面复核。正式最优方案为：

\[
\theta=0.1034366973\ \text{rad}=5.92648620^\circ,
\quad v=138.79152312\ \text{m/s},
\]

\[
t_r=0.29449793\ \text{s},\quad
\tau=0.53912361\ \text{s},\quad
t_e=0.83362154\ \text{s},
\]

\[
\mathcal I=[0.8654688980,\,5.4529022820]\ \text{s},
\quad T_{\rm eff}=4.5874333840\ \text{s}.
\]

该方案满足全部时序与速度约束；最优方案的高密度双圆周时长与完整表面复核时长一致。可视化、灵敏度分析和鲁棒性分析按当前任务范围尚未执行。
