# 问题 3：FY1 投放三枚烟幕弹干扰 M1——完整建模与求解思路

> **当前定案**：联合完整遮蔽 \(T_{\rm joint}\) 是从全域粗搜索、DE 精搜索到完整表面复核始终唯一的优化目标；独立完整遮蔽 \(T_{\rm ind}\) 及协同增益 \(\Delta T\) 仅用于解释最终方案。本文是第三问的统一口径，供建模、代码和论文写作使用。

---

## 一、问题要解决什么

FY1 在受领任务后可以瞬时确定一个航向和一个速度，之后沿该方向等高度匀速直线飞行。它最多投放三枚烟幕弹，且相邻两次投放至少间隔 1 s。要求设计：

- FY1 的航向和速度；
- 三枚弹的投放时刻；
- 三枚弹的起爆时刻（或投放到起爆的延迟）；
- 对应的投放点和起爆点。

最终目标是让三枚烟幕对 M1—真目标视线的**联合有效遮蔽总时间尽可能长**。题目明确允许遮蔽时间不连续，因此总时长按所有有效时间段的并集长度计算。

问题 3 是问题 2 的自然扩展：物理运动和单枚遮蔽评价器不变，只是从“四变量单弹优化”扩大为“八变量三弹时序协同优化”。

---

## 二、基础坐标、运动和时间

### 1. 导弹和目标

- 假目标为原点；
- 真目标是下底面圆心为 \((0,200,0)\)、半径 7 m、高 10 m 的实心圆柱；
- M1 初始位置为 \((20000,0,2000)\)，以 300 m/s 沿“初始位置指向假目标”的方向匀速直线飞行。

题面描述的是导弹飞行方向直指假目标，未要求考虑导弹重力或变速。因此采用匀速直线模型，不额外加入导弹抛体运动。

### 2. 无人机

FY1 初始位置为：

\[
F_0=(17800,0,1800).
\]

设水平航向角为 \(\theta\)，速度为 \(v\)，则：

\[
F(t)=F_0+vt(\cos\theta,\sin\theta,0).
\]

其中：

\[
0\le\theta<2\pi,\qquad70\le v\le140.
\]

同一架 FY1 的三枚烟幕弹共用 \(\theta,v\)，这是第三问与“三架无人机各投一枚”的第四问最重要的区别。

### 3. 第 \(j\) 枚烟幕弹

第 \(j\) 枚弹在时刻 \(t_{rj}\) 投放，延迟 \(\tau_j\) 后起爆：

\[
t_{ej}=t_{rj}+\tau_j.
\]

投放后至起爆前，烟幕弹继承无人机水平速度，竖直方向自由下落。其起爆点为：

\[
P_{ej}=
\left(
17800+vt_{ej}\cos\theta,
vt_{ej}\sin\theta,
1800-\frac12g\tau_j^2
\right).
\]

起爆后，球形烟幕半径固定为 \(R=10\) m，球心不再水平移动，而以 3 m/s 匀速下沉：

\[
C_j(t)=P_{ej}+(0,0,-3(t-t_{ej})),
\qquad t\in[t_{ej},t_{ej}+20].
\]

烟幕从起爆起固定有效 20 s。按题设最简口径，只要求烟幕在地面以上起爆；不额外加入“烟幕球触地后失效”的截断规则。

---

## 三、决策变量与硬约束

### 1. 推荐的代码变量

为避免直接优化 \(t_{r1},t_{r2},t_{r3}\) 时频繁违反投放间隔，代码中定义：

\[
x=(\theta,v,t_{r1},\Delta_{12},\Delta_{23},\tau_1,\tau_2,\tau_3).
\]

其中：

\[
t_{r2}=t_{r1}+\Delta_{12},
\qquad
t_{r3}=t_{r1}+\Delta_{12}+\Delta_{23}.
\]

这只是参数化方式，不改变题目含义。论文的结果表仍直接报告 \(t_{r1},t_{r2},t_{r3}\)。

### 2. 全部约束

\[
0\le\theta<2\pi,qquad70\le v\le140,
\]

\[
t_{r1}\ge0,qquad\Delta_{12}\ge1,qquad\Delta_{23}\ge1,
\]

\[
\tau_j\ge0,qquad t_{rj}+\tau_j\le13.94,qquad j=1,2,3.
\]

其中 \(t_{rj}+\tau_j\le13.94\) 是第二问已推导出的有效时序上界：超过这个时刻起爆，烟幕已不可能及时进入 M1—真目标的可遮挡视线区域。该约束同时保证起爆高度为正。

不要额外添加：

\[
t_{e1}\le t_{e2}\le t_{e3}.
\]

因为题目只规定三次**投放**的先后及间隔，没有规定三次起爆必须同序。

---

## 四、遮蔽的几何判据

### 1. 有限视线段

在时刻 \(t\)，取真圆柱表面采样点 \(Q_n\)，导弹位置为 \(M(t)\)。导弹看向该点的视线是有限线段：

\[
\overline{M(t)Q_n}.
\]

对第 \(j\) 枚烟幕定义：

\[
d_{jn}(t)=
d\bigl(C_j(t),\overline{M(t)Q_n}\bigr).
\]

若 \(d_{jn}(t)\le10\)，则第 \(j\) 枚烟幕挡住该目标点对应的视线。

### 2. 独立完整遮蔽

如果存在一枚烟幕，能独自挡住真圆柱的全部视线，则该时刻独立完整遮蔽成立：

\[
\min_j\max_n d_{jn}(t)\le10.
\]

它的计算顺序是：

1. 固定某一枚烟幕，先找圆柱上最难遮挡的点，即 \(\max_n d_{jn}(t)\)；
2. 再从三枚烟幕中找最好的那一枚，即 \(\min_j\)；
3. 若结果不超过 10 m，说明存在一枚烟幕能够独立完整遮蔽。

三枚弹独立有效时间段取并集，记总时长为 \(T_{\rm ind}\)。

### 3. 联合完整遮蔽

三枚烟幕可能各自挡住圆柱的不同部分：例如第 1 枚遮左侧视线，第 2 枚遮右侧视线。单独看谁都不能遮住完整圆柱，但合起来可以。

因此，真正的三弹联合判据为：

\[
\max_n\min_{j\in\mathcal J(t)}d_{jn}(t)\le10,
\]

其中 \(\mathcal J(t)\) 是时刻 \(t\) 仍有效的烟幕集合。

它的计算顺序与独立判据正好相反：

1. 固定一个目标点 \(Q_n\)，在有效烟幕中自动选最能遮住它的那一枚，即 \(\min_j d_{jn}(t)\)；
2. 再找全体目标点中最难处理的点，即 \(\max_n\)；
3. 若最终距离不超过 10 m，则每一个目标点至少都有一枚烟幕遮住。

得到的有效时间总长记为 \(T_{\rm joint}\)。一定有：

\[
T_{\rm joint}\ge T_{\rm ind}.
\]

因为“存在一枚烟幕单独遮完整个目标”本来就是“多枚烟幕共同遮完整个目标”的特殊情况。

---

## 五、为什么联合遮蔽必须从搜索开始就作为目标

第三问有八个连续变量。独立遮蔽 \(T_{\rm ind}\) 计算较快，但它只承认“存在某一枚烟幕独自遮住整个圆柱”的时刻。若三枚烟幕分别遮住圆柱的不同部分，可能出现 \(T_{\rm ind}\) 很小、甚至为零，而 \(T_{\rm joint}\) 很大的“纯协同方案”。

因此，若先按 \(T_{\rm ind}\) 筛掉低分区域，再到少数候选中计算联合遮蔽，就会在逻辑上主动排除这类纯协同方案。这不符合本问最终目标。

比较三种做法：

| 做法 | 优点 | 风险 |
|---|---|---|
| 全程独立遮蔽 | 计算快，区间容易处理 | 可能漏掉纯协同方案 |
| 先独立、后联合 | 实现层次简单 | 联合搜索范围受独立结果限制，仍可能漏解 |
| 全程以联合遮蔽为目标，采用分层精度 | 目标与最终答案一致，不主动排除协同方案 | 每次评价较慢，需要控制采样精度和候选数 |

正式采用第三种。

这里的“分层”只指**评价精度分层**：低成本双圆周用于全域探索，中高精度双圆周用于重排，完整圆柱表面用于最终确认；它不改变目标函数。所有搜索阶段均最大化 \(T_{\rm joint}\)。

---

## 六、正式搜索流程

### 阶段 A：全域 LHS 粗搜索

在完整八维可行域中生成可行的拉丁超立方样本（LHS），使用低成本双圆周评价器计算 \(T_{\rm joint}\)。

粗搜索可选择的方法如下：

| 方法 | 优点 | 缺点 |
|---|---|---|
| 八维规则网格 | 形式最直观 | 维数灾难，实际不可承受 |
| 纯随机抽样 | 实现简单 | 覆盖容易聚团，不够均匀 |
| LHS | 每一维分层覆盖，成本适中 | 不能保证全局最优 |

采用 LHS。它的用途是识别“好方案大概集中在哪里”，不是直接宣布某个样本最优。

从联合遮蔽时长高的样本中选取若干个彼此分散的中心，而不是机械取前若干名。航向角距离使用环形距离，避免把 \(1^\circ\) 与 \(359^\circ\) 错当成相距很远。这样既保留高分区域，也降低所有初值挤在同一局部峰附近的风险。

### 阶段 B：多区域约束 DE 精搜索

从多个粗搜索优质区域分别启动 DE。每次 DE 的变量边界始终是完整可行域；“多区域”只体现在初始种群来源，而不是人为删掉其他可行方案。

每个初始种群应由四部分构成：

- 当前区域中心附近的可行扰动方案；
- 其他优质区域中心及其轻微扰动；
- 由第二问最优单弹方案扩展而来的三弹可行基线；
- 新生成的全域可行 LHS 样本。

这一阶段直接优化：

\[
\max T_{\rm joint}.
\]

不同区域、不同随机种子分别启动 DE；所有 DE 的变量边界保持完整八维可行域。这样既保留从不同初值出发的探索能力，也不会因独立遮蔽效果较弱而放弃潜在的协同区域。

### 阶段 C：中高精度联合重排

合并各次 DE 的前若干候选，去重后用中高精度双圆周评价器重新计算：

\[
T_{\rm joint}.
\]

该阶段只解决一个问题：低精度评价器选出的候选，在更密采样下是否仍然优秀。按新的 \(T_{\rm joint}\) 结果排序后，保留前若干名进入最终复核。

### 阶段 D：完整圆柱表面最终复核

最终前若干候选必须使用完整圆柱外表面、较小时间步长和端点二分复核。时间推进方式为：

1. 扫描时间轴，识别联合有效与无效的切换；
2. 对每一个进入和退出边界用二分法精确定位；
3. 得到所有联合遮蔽区间；
4. 合并相交或仅相隔数值容差 \(\varepsilon\) 的区间；
5. 输出总遮蔽时长、最长连续遮蔽时长和最大空档。

最终正式答案以该阶段的 \(T_{\rm joint}\) 为准，而不是以 LHS 或低精度 DE 内部结果为准。

### 诊断量：独立遮蔽与协同增益

在最终前若干名确定后，再计算 \(T_{\rm ind}\) 和：

\[
\Delta T=T_{\rm joint}-T_{\rm ind}.
\]

它们不参与粗搜索、区域选择或 DE 目标函数，只用于解释最终方案：

- \(\Delta T\) 很小，说明三枚烟幕主要是各自完整遮蔽后进行时间接力；
- \(\Delta T\) 明显为正，说明三枚烟幕存在对不同视线区域的真实分工协同。

---

## 七、双圆周与完整表面的承接

真圆柱是凸集，圆柱上、下边缘圆周是关键几何边界。因此搜索阶段采用双圆周采样，能大幅减少每次评价的目标点数。

但论文中必须写清：

1. 双圆周用于快速识别优质方案；
2. 它具有较强几何代表性，但不是严格等价于完整圆柱表面；
3. 最终候选需以完整圆柱侧面、上底面、下底面的高密度采样复核。

这样，凸集说明服务于“为什么可以先快后严”，而不是把数值近似包装成严格证明。

---

## 八、输出、验证与结果口径

### 1. 需要输出的结果

最终结果至少应包括：

- \(\theta,v\)；
- 三枚烟幕弹的 \(t_{rj},\tau_j,t_{ej}\)；
- 三个投放点和三个起爆点；
- 三枚弹的联合遮蔽区间、总时长、最长连续段和最大空档；
- 作为解释量的 \(T_{\rm ind}\) 与协同增益 \(\Delta T\)；
- 按题目模板写入 `result1.xlsx`。

### 2. 必须通过的验证

- 所有方案满足速度、投放间隔、非负时间和起爆时刻约束；
- 三枚烟幕均在地面以上起爆；
- 只有一枚烟幕有效时，联合判据与独立判据必须一致；
- 对任意候选，应有 \(T_{\rm joint}\ge T_{\rm ind}\)；
- 加密双圆周后，候选排序不应严重翻转；
- 最终结果必须通过完整圆柱表面复核；
- 多随机种子应得到相近水平的优质结果，不能只展示一次偶然运行；
- 问题 1 的完整表面结果 \(1.3916426693\) s 必须持续作为共享评价器的回归测试。

### 3. 结论如何表述

LHS 和 DE 都是启发式方法，不能证明严格全局最优。正式论文中应写：

> 在给定可行域、采样精度和多区域多种子搜索条件下，得到联合完整遮蔽时长最大的推荐方案；经完整圆柱表面复核，该方案满足全部运动与时序约束。

不要写“理论全局最优”。

---

## 九、论文叙事顺序

建议第三问按以下顺序组织正文：

1. 承接问题 2：说明单枚评价器已建立并通过问题 1、2 验证；
2. 给出三枚烟幕的决策变量、运动方程和全部约束；
3. 定义独立完整遮蔽，用于解释单枚烟幕独立承担遮蔽的含义；
4. 指出独立判据会漏掉“多枚烟幕分工遮挡”，继而提出联合遮蔽判据，并将其定为唯一优化目标；
5. 说明八维空间下采用“全域 LHS—分散区域—多区域联合 DE—逐级精度复核”的原因；
6. 展示最终方案、每枚弹的投放和起爆信息、联合遮蔽区间及总时长；
7. 报告 \(T_{\rm ind}\) 与 \(\Delta T\)，解释方案属于接力遮蔽还是存在协同分工；
8. 最后说明完整表面复核、时间精度检查与启发式搜索的局限。

整段逻辑可以概括为：

\[
\text{单弹评价器}
\rightarrow
\text{三弹时序模型}
\rightarrow
\text{全域联合粗搜索}
\rightarrow
\text{多区域联合 DE}
\rightarrow
\text{严格表面复核}
\rightarrow
\text{推荐策略}.
\]

这条逻辑既能控制计算量，也不会回避三枚烟幕可能共同遮挡的物理事实。

---

## 十、当前状态和下一步

- 物理模型、变量、约束、独立与联合判据、以联合目标为唯一目标的分层算法已经确定；
- 第三问尚未运行正式优化，本文不包含任何第三问数值结论；
- 下一步应由代码实现共享三弹联合评价器，完成“联合遮蔽 LHS + 多区域联合 DE + 完整表面复核”；
- 代码、结果表与论文需要始终使用本文相同的变量定义与判据顺序。
